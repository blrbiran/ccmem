# 抬高 `openai_timeout_ms`（800 → 1200）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `DEFAULT_CONFIG.embedding.openai_timeout_ms` 从 800 抬到 1200，止住 hook 侧 48.5% 的 embed 截尾，同时让 hook 自己产出 800–1200 区间的**无截断**延迟分布。

**Architecture:** 单点常数改动 + 一条编码"为什么"的预算不变式测试 + 一份**在改动之前就冻结提交**的测量预登记。抬超时这个动作本身就是那次测量：每一条从 `B-fail` 变成 `A` 的行都带着真实 `embedMs`，落在完全正确的负载上 —— 那正是探针（偏快 1.7×，见 handoff Ⅺ.5）拿不到的东西。

**Tech Stack:** Node.js（**必须 `/usr/local/bin/node` v24.13.0**，见 handoff Ⅳ.2/Ⅳ.20）、`node:test`、`node:sqlite`、OpenAI SDK。

## Global Constraints

- **不要 push。** 删分支/worktree 先问。**不要把 plist 或配置内容打印/落盘。**
- 跑单个测试文件必须两个变量齐全：`env -u CCMEM_CONFIG_PATH CCMEM_DATA_ROOT="$(mktemp -d)" /usr/local/bin/node --test <文件>`（Ⅳ.2）。**裸 `node` 是 nvm v22.13.1，能力不同，用了会得出错误结论。**
- 每个新测试必须**亲眼看着定向变异红**，且**红在被测断言自己命名的行为上**（Ⅴ）。崩溃红、"函数不存在"红都不算。
- **本计划改的是 `DEFAULT_CONFIG`（产品默认值），不是本机 `config.json`。** 已实测确认 `~/.claude/ccmem/config.json` 里 `openai_timeout_ms` 为 `undefined`，不覆盖默认值 ⇒ 改默认值即对本机生效。
- **hook 侧下次调用即生效**（每个 prompt 一个新进程）。**daemon 需要重启才会拿到新默认值**，但 daemon 的两个 embed 调用点（`embed-latency-probe.mjs:53`、`vec-backfill.mjs:74`）**都自己覆盖了 timeout**，所以本改动**不需要重启 daemon**，也不影响探针样本的同源性。

---

## 基线（W2 冻结快照，`08-05 22:21 → 08-09 16:2x`）

改动前必须先把这几个数固定下来，它们是后面唯一的对照：

| 量 | 值 |
|---|---|
| 截尾率 `B-fail / 真实尝试` | **66/136 = 48.5%**，Wilson95 **[40.3%, 56.9%]** |
| 剔除已知跑批窗口 | 59/125 = 47.2% |
| `ms_total` | p50 924 / p90 1024 / **p99 1945** / max **2280** |
| `B-fail` 余项（`ms_total − embedMs`） | p50 161 / p90 263 / **max 1476** |
| `A` 路径成功 `embedMs`（在 800 处截尾） | n=70, p50 673 / p90 800 / max 812 |
| `error: 'timeout'` 行数（全历史 3069 条） | **0** |

取数口径见 handoff **Ⅳ.23** 与 **Ⅺ.2**（`retrieval_path` 分类；`A` 里必须剔掉 `retrieval_embed_ms === 0` 的缓存命中）。

---

## File Structure

| 文件 | 责任 | 动作 |
|---|---|---|
| `docs/superpowers/plans/2026-08-10-raise-openai-timeout-prereg.md` | **预登记**：成功判据、中止判据、n、分析口径。**必须先于常数改动提交。** | Create |
| `scripts/lib/hook-safety.mjs` | 新增导出 `LEXICAL_FALLBACK_RESERVE_MS`，让"超时之后还要跑回落"这件事有名字 | Modify（`HOOK_BUDGET_MS` 之后） |
| `tests/unit/v013-hook-timeout-budget.test.mjs` | 预算不变式的既有归属地，新增一条守住"超时 + 回落必须留在 harness 硬限内" | Modify（文件末尾追加） |
| `scripts/lib/config.mjs:18` | `DEFAULT_CONFIG.embedding.openai_timeout_ms` | Modify（800 → 1200） |
| `scripts/lib/embedding/openai.mjs:20` | 注释里写着已作废的 "800 实测到 1683ms" | Modify（更正） |
| `tests/integration/admin-diagnose-command.test.mjs:1121` | **唯一钉死默认值 800 的断言，改动后必红** | Modify |

⚠️ **不要动** `tests/unit/v013-backfill-timeout.test.mjs` —— 它第 35 行用的是**相对**比较（`backfillCfg... > DEFAULT_CONFIG...`），本改动下仍然成立；其余几处 `800` 都是显式传入的 fixture，不读默认值。

---

### Task 1：预登记测量协议（**必须在改常数之前单独提交**）

**Files:**
- Create: `docs/superpowers/plans/2026-08-10-raise-openai-timeout-prereg.md`

**Interfaces:**
- Produces: 后续分析唯一合法的判据来源。Task 4 只许照它读数，不许另立判据。

**为什么单独一个 task 且必须先提交：** handoff 反复栽在事后挪门柱上（Ⅴ"0 计数/异常数字先解释来源"、Ⅹ"前一版把 11/96 整个当成 pid=null 的频率"）。**预登记一旦和结果同轮提交，它就失去了全部约束力。**

- [ ] **Step 1: 写预登记文档**

把下面内容原样写入该文件：

```markdown
# 预登记：`openai_timeout_ms` 800 → 1200

> 本文件在常数改动**之前**提交。改动后**不得修改本文件**；
> 若判据需要修订，另开一份并说明理由，**不要就地改写**。

## 基线（W2 冻结快照，08-05 22:21 → 08-09 16:2x）
截尾率 66/136 = 48.5%，Wilson95 [40.3%, 56.9%]。

## 主要结局（止血）
**新窗口的截尾率 Wilson95 上界 < 40.3%**（即低于基线区间的下界）。
达成 ⇒ 判定"抬超时确实止住了血"；未达成 ⇒ 判定"效果不足以与基线区分"，**不许改读点估计**。

## 次要结局（取数，这才是本轮真正的产出）
新窗口 `A` 路径 `embedMs` 在 **800–1200 区间**的分布（此前完全不可观测）。
输出 n、min/p50/p90/max，以及 `P(800 < embedMs ≤ 1200)`。
⚠️ 1200 以上仍然截尾，**不许从这份分布外推 1200 以上的任何分位数**。

## 样本量（看数之前定死）
**n ≥ 150 次真实 embed 尝试**（按 W2 速率约 36/天 ⇒ 约 4–5 天）。
n < 150 时**只许报"数据不足"，不许读区间**。
若 n=150 时区间仍横跨 40.3%，**延长到 n ≥ 300 再读，不许在中途宣布结论**。

## 中止判据（任一命中即立刻回退，不等 n 攒够）
1. 出现**任何一条** `hook === 'prompt_submit'` 且 `error !== null` 的行
   （基线：全历史 3069 条全为 null；`Promise.race` 从未开火 —— 见 Ⅳ.24）。
2. 新窗口 `ms_total` 的 **p99 > 3000ms**（基线 1945ms；harness 硬限 5000ms）。
3. 出现任何一次 hook 因失败返回空 `additionalContext`。

> 🔴 **上面三条是预登记原文的忠实抄录，刻意不改。但判据 3 已于 2026-08-10 裁决为"不可操作化、未执行"** ——
> 理由与全文见 Task 5 Step 3 的说明、`…-prereg-addendum.md` 的"补遗 2 → 裁决"、handoff **Ⅺ.14**。
> **本轮实际生效的只有 1 与 2。** 读到这里就停下的人不要以为判据 3 还在跑。

## 分析口径（不许临场发明）
- 分类用 `retrieval_path`；`真实尝试 = (A − 缓存命中) + B-fail`，缓存命中判据 `retrieval_embed_ms === 0`（Ⅳ.23）。
- **分析前先把 `metrics.jsonl` 复制成冻结快照再算**（Finding 15 设计文档 line 15-17 的教训：活文件会在分析过程中被追加，导致差一不一致）。
- **剔除本机跑批窗口**，并在跑批发生时**当场记下起止时间**，不要事后回忆（Ⅰ：边界差 6 秒就把 5.1% 读成 9.2%）。
- 区间一律用 Wilson 95%。

## 预登记的失败可能
若主要结局未达成而次要结局显示 800–1200 区间里几乎没有质量，
**正确结论是"embed 延迟的质量在 1200 以上，抬到 1200 太保守"**，
而**不是**"抬超时没用"。这两者的区分依赖次要结局，所以次要结局必须照样算出来。
```

- [ ] **Step 2: 提交（此时尚未改任何代码）**

```bash
git add docs/superpowers/plans/2026-08-10-raise-openai-timeout-prereg.md docs/superpowers/plans/2026-08-10-raise-openai-timeout.md
git commit -m "docs(plan): pre-register the timeout-raise measurement before touching the constant"
```

---

### Task 2：把"超时 + 回落必须留在 harness 硬限内"变成一条测试

**Files:**
- Modify: `scripts/lib/hook-safety.mjs`（在 `PROMPT_SUBMIT_BUDGET_MS` 导出之后）
- Modify: `tests/unit/v013-hook-timeout-budget.test.mjs`（文件末尾追加）

**Interfaces:**
- Consumes: `HOOK_BUDGET_MS`、`DEFAULT_CONFIG`
- Produces: `LEXICAL_FALLBACK_RESERVE_MS: number`（从 `scripts/lib/hook-safety.mjs` 导出）

**为什么是对着 harness 的 5000 断言，而不是内部预算的 2000：** 实测全历史 3069 条 `prompt_submit` 的 `error` 全为 `null` —— 那个 2000ms 的 `Promise.race` **一次都没开过火**，因为它只框得住异步工作而 hook 大半是同步的（`hook-safety.mjs:10-17` 自己写着）。**把从不生效的软闸当硬不变式，会写出一条测的不是真实约束的测试**（Rule 9）。

- [ ] **Step 1: 在 `hook-safety.mjs` 里加常数**

在 `export const PROMPT_SUBMIT_BUDGET_MS = HOOK_BUDGET_MS.prompt_submit;` 之后插入：

```javascript
/**
 * How much wall clock must still be available AFTER the embed call gives up.
 * A timed-out embed is not the end of the hook: prompt_submit then runs the
 * whole lexical fallback and writes its context file, and that work is
 * synchronous, so no budget can interrupt it — only the harness kill can.
 *
 * 1500 is the measured max of (ms_total - embedMs) across the B-fail rows of
 * the 2026-08-05..08-09 window (observed max 1476ms, p50 161, p90 263).
 * It is deliberately the MAX and not a quantile: this reserve exists to keep
 * the worst case inside the harness timeout, and the harness kill is not a
 * budget that degrades gracefully — it takes stdout and the metrics row with it.
 */
export const LEXICAL_FALLBACK_RESERVE_MS = 1500;
```

- [ ] **Step 2: 写失败测试**

在 `tests/unit/v013-hook-timeout-budget.test.mjs` 末尾追加（`DEFAULT_CONFIG` 需要新增 import）：

```javascript
import { DEFAULT_CONFIG } from '../../scripts/lib/config.mjs';
import { LEXICAL_FALLBACK_RESERVE_MS } from '../../scripts/lib/hook-safety.mjs';

/**
 * Raising openai_timeout_ms is safe right up until the point where a timed-out
 * embed plus its lexical fallback can outlive the harness's own kill. Past that
 * line the failure mode changes character: instead of degrading to lexical
 * retrieval, the hook is killed before it can write stdout or its metrics row,
 * so the incident is also invisible afterwards. The internal 2000ms budget does
 * NOT protect this — it has never fired in 3069 prompt_submit rows because it
 * cannot interrupt synchronous work. The harness timeout is the only real limit.
 */
test('a timed-out embed plus its lexical fallback still fits inside the harness kill', () => {
  const hooksJson = JSON.parse(readFileSync(path.join(repoRoot, 'hooks/hooks.json'), 'utf8'));
  const harnessMs = findHookTimeoutMs(hooksJson, 'UserPromptSubmit');
  assert.ok(harnessMs > 0, 'UserPromptSubmit must declare a harness timeout');

  const worstCase = DEFAULT_CONFIG.embedding.openai_timeout_ms + LEXICAL_FALLBACK_RESERVE_MS;
  assert.ok(
    worstCase < harnessMs,
    `an embed that times out at ${DEFAULT_CONFIG.embedding.openai_timeout_ms}ms still owes the `
    + `lexical fallback up to ${LEXICAL_FALLBACK_RESERVE_MS}ms, so the worst case is ${worstCase}ms, `
    + `which must stay under the ${harnessMs}ms harness kill`
  );
});
```

⚠️ **`findHookTimeoutMs` 不是新函数** —— 该文件已经在读 `hooks/hooks.json`（顶部已 import `readFileSync` / `path` / `repoRoot`）。**先读该文件现有的解析辅助函数并复用它**；若现有代码是内联解析的，就把那段内联逻辑照原样用，**不要新造一个同名函数**（Rule 8：先读再写）。

- [ ] **Step 3: 定向变异 —— 看着它红，且红在自己命名的行为上**

临时把 `scripts/lib/config.mjs:18` 改成 `openai_timeout_ms: 3600`，然后：

```bash
env -u CCMEM_CONFIG_PATH CCMEM_DATA_ROOT="$(mktemp -d)" /usr/local/bin/node --test tests/unit/v013-hook-timeout-budget.test.mjs
```

Expected: FAIL，消息形如 `...worst case is 5100ms, which must stay under the 5000ms harness kill`。
**必须核对红的是这句话，不是崩溃红、不是 import 失败。** 看到之后**把 3600 改回 800**（此时还没到 Task 3）。

- [ ] **Step 4: 确认改回 800 后为绿**

```bash
env -u CCMEM_CONFIG_PATH CCMEM_DATA_ROOT="$(mktemp -d)" /usr/local/bin/node --test tests/unit/v013-hook-timeout-budget.test.mjs
```

Expected: PASS（800 + 1500 = 2300 < 5000）。

- [ ] **Step 5: 提交**

```bash
git add scripts/lib/hook-safety.mjs tests/unit/v013-hook-timeout-budget.test.mjs
git commit -m "test(hook-budget): pin the embed timeout against the harness kill, not the budget that never fires"
```

---

### Task 3：抬常数，并修好被它带红的那一条与那句过时注释

**Files:**
- Modify: `scripts/lib/config.mjs:18`
- Modify: `scripts/lib/embedding/openai.mjs:20`
- Modify: `tests/integration/admin-diagnose-command.test.mjs:1121`

- [ ] **Step 1: 先确认那条钉死断言此刻是绿的（改之前的对照）**

```bash
env -u CCMEM_CONFIG_PATH CCMEM_DATA_ROOT="$(mktemp -d)" /usr/local/bin/node --test tests/integration/admin-diagnose-command.test.mjs
```

Expected: PASS。**这是正面对照** —— 证明下一步的红确实由常数改动引起。

- [ ] **Step 2: 改常数**

`scripts/lib/config.mjs:18`：

```javascript
    openai_timeout_ms: 1200,
```

- [ ] **Step 3: 跑那个集成测试，看着它红**

Expected: FAIL 在 `assert.match(raw, /"openai_timeout_ms": 800/)`。**这条红是预期的，它证明该断言确实在守着默认值。**

- [ ] **Step 4: 更新该断言**

`tests/integration/admin-diagnose-command.test.mjs:1121`：

```javascript
  assert.match(raw, /"openai_timeout_ms": 1200/);
```

- [ ] **Step 5: 更正 `openai.mjs:20` 那句已作废的注释**

原文写着 `openai_timeout_ms: 800 measured 1683ms of wall clock on the ...`。该句描述的是 `maxRetries: 0` 落地**之前**的状态（Finding 15 设计文档 §附带确认 1 已指出），且 800 也不再是默认值。改为：

```javascript
    // of itself. Historical note: before maxRetries was pinned to 0 (2026-08-02),
    // a nominal 800ms cap was measured at 1683ms of wall clock — one timeout plus
    // one retry. The default moved to 1200ms on 2026-08-10; see
    // docs/superpowers/plans/2026-08-10-raise-openai-timeout.md for the evidence.
```

- [ ] **Step 6: 跑全量套件**

```bash
npm test 2>&1 | tail -20
```

Expected: 全绿。⚠️ **如实报数**：写下 `pass` / `fail` 的实际数字。若有红，**先逐条分类失败模式再归并**（Ⅹ 的教训），并对照 handoff Ⅹ 的已知抖动谱 —— 但**"它在清单上"不是解释**（Ⅲ.3）。

- [ ] **Step 7: 提交**

```bash
git add scripts/lib/config.mjs scripts/lib/embedding/openai.mjs tests/integration/admin-diagnose-command.test.mjs
git commit -m "feat(embedding): raise openai_timeout_ms from 800 to 1200"
```

---

### Task 4：确认新值真的到了 hook 路径上（不要信改完就算数）

**Files:** 无（只读验证）

**为什么需要这一步：** handoff Ⅲ.6 记着一次"配置加了却完全不生效，且静默"。**改完必须看到活的证据。**

- [ ] **Step 1: 记下当前 `metrics.jsonl` 的行数作为分界点**

```bash
wc -l ~/.claude/ccmem/metrics.jsonl
```

写进本计划的执行记录里。**它是"新窗口"的起点。**

- [ ] **Step 2: 正常用几次 Claude Code（不需要构造，日常使用即可），然后确认新行的形态**

```bash
/usr/local/bin/node -e '
const fs=require("fs"),os=require("os");
const R=fs.readFileSync(os.homedir()+"/.claude/ccmem/metrics.jsonl","utf8").split("\n").filter(Boolean).map(JSON.parse)
 .filter(r=>r.hook==="prompt_submit").slice(-20);
for(const r of R) console.log(new Date(r.ts).toLocaleString("sv"), r.retrieval_path, "embedMs="+r.retrieval_embed_ms, "err="+r.error);
'
```

**判据（两条都要满足才算通）：**
- 出现**至少一条** `retrieval_path === "A"` 且 `retrieval_embed_ms > 812` 的行 ⇒ **新上限确实生效了**（812 是旧窗口 `A` 路径的实测 max，旧值下不可能超过它）。
- 出现的 `B-fail` 行其 `retrieval_embed_ms` 落在 **1200 附近**而不是 800 附近。

⚠️ **不要从缺失下结论**（Ⅴ）：若一段时间内没有 `A` 超过 812 的行，那可能只是延迟本来就没那么高，**不等于改动没生效**。此时改用第二条判据（`B-fail` 的截尾点）来判。若连 `B-fail` 都还是 800 附近，**才是真的没生效** —— 回去查 Ⅲ.6（配置层级）与 Ⅳ.4。

- [ ] **Step 3: 把分界点与实测样本记进 handoff Ⅺ**，并**不提交**任何结论性的截尾率数字（n 还不够，预登记说了 n<150 只许报"数据不足"）。

---

### Task 5：（**约 4–5 天后**，n ≥ 150 时才执行）按预登记读数

> 🎉 **本 Task 已于 2026-08-26 执行完毕，主要结局 `达成`（两臂一致）。报告见 handoff ⅩⅩⅢ。**
> 快照 `~/.claude/ccmem/task5-snapshot-20260826.jsonl`，sha256 `81c3f6c1…`，7420 行 / 0 解析失败。

- [x] **Step 1: 把 `metrics.jsonl` 复制成冻结快照再分析**（活文件会在分析过程中被追加）
- [x] **Step 2: 严格照 `2026-08-10-raise-openai-timeout-prereg.md` 的口径算**，不新增判据
- [x] **Step 3: 每天至少看一次中止判据 1 和 2**，不要等到第 5 天才发现要回退

  ⚠️ **本轮实际生效的中止判据只有 1 与 2。判据 3 已于 2026-08-10 裁决为"不可操作化、未执行"** ——
  它钉的 `additional_context_empty` 测的是"走没走文件通道"而不是"上下文是否为空"，照字面读 100% 误命中，
  且三个候选代理在健康基线上以 6.9%–100% 的频率恒常开火，换不出能用的。
  裁决全文见 `2026-08-10-raise-openai-timeout-prereg-addendum.md` 的"补遗 2 → 裁决"与 handoff **Ⅺ.14**。
  **读数报告必须显式写出"判据 3 从未执行"，不得默认它通过了。预登记本体零改动。**
  ⚠️ 判据 2 在 `n < 150` 时只作**离群监视**（看 max），**不算 p99**。
- [x] **Step 4: 把结果写进 handoff Ⅺ**（⇒ 实际写进 **ⅩⅩⅢ**，Ⅺ 处留了指针），成功与否都要写；**若主要结局未达成，按预登记的"失败可能"那段判断是"没用"还是"太保守"**

**回退（任一中止判据命中时）：** `scripts/lib/config.mjs:18` 改回 `800`，撤销 `admin-diagnose-command.test.mjs:1121` 的断言，跑一次全量，提交。**Task 2 的不变式测试保留** —— 它与本次取值无关，是长期护栏。

---

## Self-Review

**1. Spec coverage** —— 你（人类）批准的三件事：抬 800→1200（Task 3）、按 Ⅷ 先出计划（本文档）、预先登记成功/中止判据与 n（Task 1）。**三项都有归属 task。**

**2. Placeholder scan** —— 无 TBD / "适当处理错误" / "类似 Task N"。唯一一处非字面量是 Task 2 的 `findHookTimeoutMs`，**已显式要求先读现有文件复用而不是新造**，并说明了理由。

**3. Type consistency** —— `LEXICAL_FALLBACK_RESERVE_MS` 在 Task 2 Step 1 定义、Step 2 使用，名字一致；`DEFAULT_CONFIG.embedding.openai_timeout_ms` 的路径与 `tests/unit/v013-backfill-timeout.test.mjs:35` 现有用法一致。

## 已知不做的事

- **不抬内部预算 2000ms**（harness 给 5000，空着 3000）。那是独立的设计裁决，捆进来会让本次改动不可归因（Rule 3 surgical）。
- **不动探针**（样本同源约束仍在；探针的修法是改成冷路径取样，属 B 线）。
- **不解决冷启动 ~220ms**（B 线，攻因；本计划是止血 + 取数）。

---

## 🔴 本机跑批窗口记录（Task 5 读数时必须剔除）

预登记的分析口径要求"**剔除本机跑批窗口**，并在跑批发生时**当场记下起止时间**，不要事后回忆"。
这里是被 git 跟踪的那一份 —— `.superpowers/sdd/` 里的 ledger 是 git-ignored 的，`git clean -fdx` 会抹掉它。

**2026-08-11**（执行 W4 daemon 成本计量 + A1/A2 测试路径修复，全部为 `npm test` 全量跑）：

| 起 | 止 | 位置 | 结果 |
|---|---|---|---|
| 21:09:58 | 21:10:15 | worktree `w4-daemon-cost` | 547 pass |
| 21:31:32 | 21:31:49 | worktree `w4-daemon-cost` | 566 pass |
| 21:43:23 | 21:43:40 | worktree `w4-daemon-cost` | 572 pass |
| 22:03:49 | 22:04:06 | worktree `w4-daemon-cost` | 582 pass |
| 22:22:40 | 22:22:57 | worktree `w4-daemon-cost` | 582 pass |
| 22:24:59 | 22:25:15 | `main`（合并后验证） | 582 pass |

🔴 **生效的剔除口径（补遗 3，2026-08-12 裁决）—— 窄义**：
**只剔"全量套件运行期 + 前后各至少 60s 余量"**，即上表六行各自外扩 ≥60s。
留余量的理由不变（Ⅰ 记过：边界差 6 秒就把 5.1% 读成 9.2%），**变的是不再按"会话是否活跃"整段剔。**

> ~~⚠️ **不要只剔这六个 17 秒的窗口。** 按 handoff Ⅰ 的规则，窗口按"**会话是否活跃**"划、~~
> ~~不按"批次是否在跑"划，批次边界一律留至少 60s 收尾余量。~~
> ~~**本会话 2026-08-11 全天活跃，按 Ⅰ 的口径整段属"曝露不明"。**~~
>
> ⬆️ **以上三行已于 2026-08-12 随补遗 3 作废，保留原文以便看见判断是怎么变的。**
> 作废理由：**Ⅰ 的"整天曝露不明"是给探针定的**（探针 5 分钟节流，"会话活跃"对它有区分力）；
> 而 `prompt_submit` **按构造每 prompt 一行 ⇒ 每一行都产生于活跃会话**，整天义对它零区分力，
> **照做等于把全部数据剔光**。另：基线 `59/125` 本身就是按窄义算的（handoff Ⅺ.3，只剔了 11 行），
> 新窗口按窄义才与它同构。全文见 `…-prereg-addendum.md` 补遗 3 与 handoff ⅩⅢ.1。
> ⚠️ **`.superpowers/sdd/2026-08-10-raise-openai-timeout/progress.md` 末尾那份同源表仍带作废前的措辞**
> （它是 git-ignored 的历史 ledger，刻意不回改）—— **以本节为准。**

📌 W4 的成本行写在独立的 `daemon-cost.jsonl`，**不进 `metrics.jsonl`** ——
本轮改动本身不往窗口里加行，污染只来自跑批的机器负载。

### ✅ 窗口全程的批次覆盖完整性（2026-08-12 核查）

上表只有 08-11 的六行，而窗口从 **08-10 01:53:30** 就开了。**核查结论：这不是记录缺失，是那两段真的没跑批。**
证据一律取自**当时留下的记录**（预登记禁止事后回忆），逐条如下：

| 时段 | 结论 | 同期证据 |
|---|---|---|
| 08-10 01:53:30（窗口开）→ 08-10 全天 | **无套件** | 合并后那次 `547/547` 跑在**窗口起点之前**（ledger 里 "Suite re-run ON THE MERGE RESULT" 写在 "MEASUREMENT WINDOW START" 之前，起点正是据此定的）；Task 4 记为 "**no suite run**"；当天两个开放问题记为 "read-only, nothing fixed, **no tests run**" |
| 08-10 09:01 → 08-11 20:02 | **无开发会话** | 该区间 `git log --all` **零提交**（上一条 `c0cf156` 08-10 09:01:23，下一条 `a9e0cdb` 08-11 20:02:11） |
| 08-11 20:02 → 22:25 | **六次，已记录** | 即上表；首次跑批 21:09:58 与其后 21:10:42 的提交吻合 |
| 08-12 00:18–00:28、00:44– | **无套件**（只读分析） | 见下节「每日巡检记录」两次的补充记录 |
| 🆕 08-25 01:04:27–01:05:00、01:05:16–01:05:17 | **两次单文件单元测试**（**不是全量套件**） | 当场记，见下方补记 |

**🆕 2026-08-25 01:04–01:05 那两次的补记（当场记，非事后回忆）：**

- **跑的是什么**：`tests/unit/plugin-manifest.test.mjs`（两次：先看它红，改完再看它绿）
  与 `tests/unit/v013-optional-import.test.mjs`（一次）。**没有跑 `npm test` 全量套件。**
- **为什么跑**：修 `.claude-plugin/plugin.json` 的版本漂移（`0.2.0` vs tag `v0.13.1`）与缺失的
  marketplace 字段，**人类 2026-08-25 明确批准"允许跑一次单元测试验证"**。
- **时长**：两段合计 **34 秒**（`01:04:27→01:05:00`、`01:05:16→01:05:17`），三次 node 进程各亚秒。
- ⚠️ **按补遗 3 的窄义，这构成"本仓库测试运行"** —— 虽然只是单文件、亚秒级，
  **但它落在窄义剔除的定义里**，所以**如实记进本表**，由读数时按口径处置。
  🔴 **不要因为"只有 34 秒"就替读数决定它不用剔** —— 那是读数的事，不是巡检的事。
- **改动本身不往 `metrics.jsonl` 加行**（改的是 `plugin.json` / `package.json` / 一个测试文件），
  污染只来自这 34 秒的机器负载。

⚠️ **这条结论的效力边界，照 Ⅺ.14 的写法明说**：第二行是**从"零提交"推出"没跑批"**，
而"跑了套件但没提交"在原理上可能发生。**它是同期证据里最强的一条，不是直接观测。**
若读数时对某一小段存疑，**正确处置是把该段一并剔除（保守方向），不是回忆当时干了什么。**

**2026-08-26**（W0 落地后的全量套件，`npm test` 全量跑，1 次；🔴 **预登记测量窗口已于 2026-08-26 关闭、
Task 5 读数已完成、主要结局已裁定** —— 本条记录不再是剔除输入，**只是审计留痕**，照纪律当场记一笔）：

| 起 | 止 | 位置 | 结果 |
|---|---|---|---|
| 21:18:24 | 21:18:48 | `main` checkout，分支 `w0-non-default-config-reporting` | 605 pass |
| 21:50:51 | 21:51:08 | `main` checkout，分支 `w0-non-default-config-reporting` | 606 pass |
| 23:13:56 | 23:14:15 | `main`（合并后验证） | 606 pass |

🆕 **第 2 行是交接前的复跑**（`finishing-a-development-branch` 要求在**即将交付的那棵树上**跑一次）。
本计划原写「只在 Task 6 跑一次」，那条规则的目的是**别污染测量窗口**；窗口已于 2026-08-26 关闭 ⇒ 目的已消失。
而第 1 行那次之后，最终 review 的修复动了两个**测试文件** ⇒ `605` 那个数**已不描述当前这棵树**。
✅ `605 → 606` 与「修复波新增 1 条单元测试」逐一对上，是一次交叉验证，不是意外漂移。
⚠️ **两次跑之间没有任何产品代码改动**（只有测试文件与文档），这一点是上面那句能成立的前提。
🆕 **第 3 行是合并到 `main` 之后在合并结果上的复跑**（`finishing-a-development-branch` 要求：
绿只证明它跑过的那棵树）。`606 = 606` ⇒ 合并没引入任何东西，与 `2026-08-11` 那组最后一行
「`main`（合并后验证）」的做法一致。

**🆕 2026-08-26 21:30–21:33 的补记（最终 review 阶段，当场记，非事后回忆）：**

- **21:32:39–21:32:40**：`tests/integration/v014-diagnose-config.test.mjs` **刻意不带** `env -u CCMEM_CONFIG_PATH`
  跑了一次——正是这次暴露了 Fix 2 修的那个洞。`CCMEM_CONFIG_PATH` 被继承时：
  承载验收判据 3、4 的 **5 个 spawned 测试全部失败**；**4 个 in-process 测试不受影响**，因为它们直接
  注入 `cfg`，根本不走到 `loadConfig()`；**第 6 个 spawned 测试也通过了，但通过得没有意义**——它只断言
  注入值不出现，而在这个 bug 下那个值根本没被读取，所以它的通过不能证明任何东西。
  另跑了一次单元测试文件，**11/11 pass**。
- **约 21:30–21:33**：另有几次亚秒级的 `node -e` 探针，直接调用纯模块 `config-delta.mjs`（不经 CLI、不经 diagnose）。
- ⚠️ 以上全是**单文件、亚秒级**的运行，**按补遗 3 的窄义不构成需剔除的跑批窗口**——
  且**测量窗口已于 2026-08-26 关闭**，这条记录**只是审计留痕**，不影响任何已裁定的读数。

**2026-08-27**（W1「隔离全部来源」全量套件跑批，`npm test` 全量跑，**4 次**；🔴 预登记测量窗口已于 2026-08-26 关闭、
Task 5 读数已完成、主要结局已裁定 —— 本条记录**不是剔除输入，只是审计留痕**，照纪律当场记一笔）：

| 起 | 止 | 位置 | 结果 |
|---|---|---|---|
| 09:28:29 | 09:28:58 | `main` checkout，分支 `w1-quarantine-all-sources` | 617 pass |
| 21:30:45 | 21:31:12 | `main` checkout，分支 `w1-quarantine-all-sources` | 619 pass |
| 21:56:38 | 21:56:55 | `main` checkout，分支 `w1-quarantine-all-sources` | 619 pass |
| 23:25:36 | 23:25:52 | `main`（合并后验证） | 619 pass |

沿用补遗 3 的窄义剔除口径：本行跑批窗口前后各留 ≥60s 余量（即便本条已不再是活的剔除输入，记录时仍照此口径留痕，纪律不因窗口关闭而放松）。

第三、四行是 `finishing-a-development-branch` 要求的两次：**第三行**跑在即将交付的那棵树上（最后一次提交只动了两个**测试文件里的注释与字符串字面量**，故 `619` 需要重新证明它仍描述这棵树），**第四行**跑在**合并结果**上。`619 = 619` ⇒ 合并没引入任何东西。

第二行是**完整代码评审后的修复轮**跑批，不是新的测量输入（窗口早已于 08-26 关闭，读数与主要结局都已裁定，这里同样只是审计留痕）。计数从 617 → 619，**+2**：本轮新增两个 `test()` 块，均在 `tests/unit/v014-quarantine-all-sources.test.mjs`——钉住 `DEFAULT_CONFIG.security.quarantine_all_sources_at_write` 与 `config.default.json` 同键的出厂默认值必须为 `false`（评审 Important 1）。同轮另有一条断言加进了 `tests/integration/v014-quarantine-all-sources-write.test.mjs` 的既有 `test()` 里（审计行 `security_quarantine_in`，评审 Minor 5），不新增 `test()` 块，因此不计入这 +2。617 + 2 = 619，与观测到的通过数一致，无需对账偏差。

**2026-08-28**（W2「按开关切换 scope 隔离」全量套件跑批，`npm test` 全量跑，**2 次**；🔴 预登记测量窗口已于 2026-08-26 关闭、Task 5 读数已完成、主要结局已裁定 —— 本条记录**不是剔除输入，只是审计留痕**，照纪律当场记一笔；📌 **窗口已闭，仅存档**）：

| 起 | 止 | 位置 | 结果 |
|---|---|---|---|
| 22:26:06 | 22:26:32 | `main` checkout，分支 `w2-scope-isolation-switch` | 641 pass |
| 22:55:59 | 22:56:17 | `main` checkout，分支 `w2-scope-isolation-switch` | 647 pass |

沿用补遗 3 的窄义剔除口径：本行跑批窗口前后各留 ≥60s 余量（即便本条已不再是活的剔除输入，记录时仍照此口径留痕，纪律不因窗口关闭而放松）。

第 1 次跑批是 Task 7 明文规定的那一次（计划原写「全篇唯一一次」，因修复波改了测试文件而实际跑了两次，见下）。计数从 619 → 641，**+22**：本轮新增三个测试文件 —— `tests/unit/v014-scope-isolation-defaults.test.mjs`（+2：`disable_scope_isolation defaults to false`、`config.default.json carries the same value`）、`tests/integration/v014-scope-isolation-retrieval.test.mjs`（+10：`ftsSearch`/`likeSearch`/`legacySubstringSearch` 各 2 条「开关关时隔离 / 开关开时跨项目」+ 4 条 embedding 路径直调）、`tests/integration/v014-scope-isolation-injection.test.mjs`（+10：session-start 读取范围 2 条、stderr 警告 4 条、`recent_injections` 写入 2 条、`prompt-submit` 抑制 2 条）。619 + 22 = 641，与观测到的通过数（`pass 641 / fail 0 / skipped 0 / todo 0`）一致，无需对账偏差。

🔴 **第 2 次跑批（`22:55:59 → 22:56:17`，647 pass）**：整分支 review 的修复波补了 6 条测试之后的复跑。
原计划只跑 1 次，**修复波改了测试文件就必须复跑**，否则最后一次全量证据对应的不是最终代码。
计数 641 → 647，**+6**：`v014-scope-isolation-retrieval.test.mjs` 10 → 15（+5：`lexicalRetrieve` 三个调用点所在的 `B-off` 默认路径 3 条、`eval` 非对象时读法必须 fail-safe 2 条）、
`v014-scope-isolation-injection.test.mjs` 10 → 11（+1：session-start 侧同一条 fail-safe 读法）。641 + 6 = 647，与 `pass 647 / fail 0 / skipped 0 / todo 0` 一致。

---

## ✅ 读数脚本已冻结（2026-08-12，`n` 原始 137，读数之前）

**`2026-08-10-raise-openai-timeout-readout.mjs`（与本文件同目录，已进 git）。**
写它的理由和补遗一样：**分析里的每一个选择都是自由度，只有在结局未知时定死才算数。**

**用法**：`node …-readout.mjs --selfcheck`（只跑自检） / `node …-readout.mjs <冻结快照路径>`。
⚠️ **套件只 glob `tests/`，这个文件不会被扫到 —— 窗口期零时序影响。**

它把下列东西全部编码进去，**读数时只剩"复制冻结快照 + 跑一次"，没有临场判断**：

| 来源 | 已编码 |
|---|---|
| 补遗 1 | `z = 1.959963984540054`（禁用 1.96）、无连续性校正、Wilson 公式逐字；上界报 4 位小数；对 `40.3%` 与 `40.2862%` **各比一次，一致才判** |
| 补遗 2 | 判据 3 **每次都打印"不可操作化、未执行"**，且拒绝静默替换 |
| 补遗 3 | `n` 数**剔除后**；剔除取**窄义**（上表六窗各外扩 60s）；`真实尝试 = (A − 缓存命中) + B-fail`，`B-circuit/B-off` 不进分母 |
| 补遗 4 | 对 `38.7%` 的敏感性检查；**与主判据不一致 ⇒ 报"不可判定"**，不许择一 |
| 预登记 | `n<150` **只打印"数据不足"**，连截尾率和区间都不算；区间横跨 40.3% 且 `n<300` ⇒ 判"延长" |
| Ⅻ.5 | 行选择用 `ts` 不用行号，并在报告里写明；`.1` 存在时一并读 |

**已实测验证过的守卫**（合成数据，未碰真实数据）：
自检精确重现基线两端点（`0.4028624249 / 0.5685337545`）；指向活文件时**拒绝运行并退出 1**；
窗口前的行被 `ts` 滤掉；跑批窗口内的行被剔除；缓存命中与 `B-off` 不进分母；
`n<150` 时确实只输出"数据不足"；达成 / 延长 / 不可判定三条判决分支各自走通。

### ✅ 那两条已于 2026-08-13 裁决（补遗 5），**脚本已按裁决改并重验**

> ~~### 🔴 两条刻意不自己决定、留给人类的（候选补遗 5，**必须在脚本跑真实数据之前裁决**）~~
>
> ~~1. **分位数方法从未钉死**……脚本的处置是**同时算 nearest-rank 与线性插值并都打印**，~~
>    ~~两者落在 3000ms 两侧时报"不可判定"。**这是报告时的护栏，不是裁决。**~~
> ~~2. **补遗 4 把敏感性阈值钉成显示值 `38.7%`**……上界若落在带内，按哪个值判**无人裁决**。~~
>
> ⬆️ **原文划掉保留，以便看见判断是怎么定的。以下为生效版本。**

1. ✅ **分位数方法 = nearest-rank**（补遗 5.1）。**不是挑的，是反推的**：在能重现基线 `n=136`
   的 attempts 集上，nearest-rank 给 p99 **1945.4**（＝冻结的 1945），线性插值给 **1718.8**；
   起点逐分钟 ×5 种行集合共 360 组扫描中，**nearest-rank 重现 360 组、线性插值 0 组**。
   ⇒ 冻结的 1945 是**真实观测点**。脚本现在**判定只认 nearest-rank**，线性插值降级为信息项，
   两法分居 3000ms 两侧时**只报告、不再报"不可判定"**。
   🔴 **同时记一条负面结果**：基线表的 `p50 924` / `p90 1024` **在任何组合下都重现不出来**，
   来源不明。**已核实的只有 `p99` 与 `max`，不要把整张表当已复核。**
2. ✅ **`38.7%` 与未舍入的 `38.6631%` 各比一次，一致才判，不一致报"不可判定"**（补遗 5.2）——
   与补遗 1 处理 `40.3%` 的规则逐字对称。带宽 0.0369pp，真正绑定的概率约 0.2%。

📌 **脚本冻结后又被改，为什么不算破坏冻结**：改动发生在**读数之前**、内容**就是补遗 5 本身**
⇒ 自由度是被关小的。纪律三条（补遗先提交、`--selfcheck` 重跑、合成数据验分支）见补遗 5 末尾。
**已实测**：自检仍精确重现 `0.4028624249 / 0.5685337545`；六条判决分支全部走通
（含新增的敏感性分歧分支，上界 38.6734% 落在带内 ⇒ 不可判定）；
造一个两法分居 3000ms 两侧的快照（nearest-rank 3200 / linear 2073）确认**报 HIT**；
活文件守卫仍拒跑退出 1。**全程未读任何真实窗口数据。**

📌 **另一件值得先知道的事**：`38.7%` 与 `40.3%` 之间有 **1.6pp** 的宽带,
**上界落进这条带里就会触发补遗 4 的"不可判定"** —— 这个分支的触发概率**不低**，不是理论可能性。

---

## 🔴 每日巡检记录（中止判据 1 与 2）

预登记要求窗口期内持续巡检中止判据。`.superpowers/` 是 git-ignored，所以记在这里。

**巡检的边界（每次都照这个做，别扩）**：只算判据 1 与 2，以及解封闸门 `n`。
**刻意不算截尾率、不算 Wilson 区间** —— 那是 Task 5，被 `n ≥ 150` 挡着。
判据 3 **不可操作化、未执行**（人类裁决 2026-08-10，见 `…-prereg-addendum.md` 补遗 2），
**每次巡检都要照写一遍，不得默认它通过**。

| 巡检时刻 | 窗口已开 | 判据 1（`prompt_submit` 且 `error !== null`） | 判据 2（`ms_total`） | 判据 3 | `n`（原始） |
|---|---|---|---|---|---|
| 2026-08-11 23:36 | 45.7 h | **0 命中 ✅** | **0 条 >3000ms**；max **2864ms**、>2000ms 共 3 条 | 未执行 | 114 |
| 2026-08-12 00:45 | 46.9 h | **0 命中 ✅** | **0 条 >3000ms**；max **2864ms**、>2000ms 共 3 条（**与上一次同 3 条，无新增**） | 未执行 | 137 |
| 2026-08-13 23:19 | 93.4 h | **0 命中 ✅** | **0 条 >3000ms**；max **2864ms**（**未变**）、>2000ms 共 **4 条** —— 🆕 **新增 1 条：08-13 21:20:35 / 2266ms** | 未执行 | **176** |
| 2026-08-19 00:08 | 214.2 h | **0 命中 ✅** | **0 条 >3000ms**；max **2864ms**（**仍未变**）、>2000ms 仍 **4 条**（**逐条与上次相同，5 天零新增**） | 未执行 | **200** |
| 2026-08-19 23:31 | 237.6 h | **0 命中 ✅** | **0 条 >3000ms**；max **2864ms**（**仍未变**）、>2000ms 仍 **4 条**（**逐条与上次相同**） | 未执行 | **214** |
| 2026-08-20 23:05 | 261.2 h | **0 命中 ✅** | **0 条 >3000ms**；max **2864ms**（**仍未变**）、>2000ms 仍 **4 条**（**逐条与上次相同**）；增量内 max **1832ms** | 未执行 | **222** |
| 2026-08-23 10:02 | 320.1 h | **0 命中 ✅** | **0 条 >3000ms**；max **2864ms**（**仍未变**）、>2000ms 仍 **4 条**（**逐条与上次相同**）；增量内 max **1593ms** | 未执行 | **248** |
| 2026-08-25 00:02 | 358.1 h | **0 命中 ✅** | **0 条 >3000ms**；max **2864ms**（**仍未变**）、>2000ms 仍 **4 条**（**逐条与上次相同**）；增量内 max **1558ms** | 未执行 | **263** |
| 2026-08-25 21:11 | 379.3 h | **0 命中 ✅** | **0 条 >3000ms**；max **2864ms**（**仍未变**）、>2000ms 仍 **4 条**（**逐条与上次相同**）；增量内 max **1735ms** | 未执行 | **287** |

**2026-08-11 23:36 那次的补充记录：**

- 文件完整性：`metrics.jsonl` 6809 行、0 解析失败、`ts` 严格单调、**无 `.1` 轮转**。
- ⚠️ 判据 2 的 max 2864ms 距硬判据 3000ms 只差 5%（基线 p99 为 1945ms）。**这不是命中**，
  但**后续每次巡检都要看它有没有成群** —— 单点离群与分布右移是两回事。
- 方法：读的是**活文件**，不是冻结快照。对判据 1/2 这种布尔/离群判定可以接受；
  **Task 5 读数仍必须先复制成冻结快照**（预登记的分析口径，不得放宽）。
- 本次会话（08-11 深夜）**未跑任何全量套件**，只做只读分析。但机器负载仍属会话活跃。

**2026-08-12 00:45 那次的补充记录：**

- ⚠️ **距上一次巡检只有 1.15 小时，不是一个独立的"每日"数据点。** 上一次记在 08-11 23:36，
  本次是新会话开工时按纪律先做的。**08-12 当天应当在稍晚再做一次真正跨日的巡检。**
- **判据 2 的成群检查（本轮巡检的重点）**：>2000ms 的 3 条**逐条与上次相同** ——
  `08-11 09:26:59 / 2112ms`、`08-11 21:16:03 / 2525ms（stop）`、`08-11 22:25:34 / 2864ms`。
  期间文件新增 68 行、窗口内新增 23 次真实尝试，**0 条新的 >2000ms**。
  ⇒ 目前证据支持"2864ms 是单点离群"，**不支持分布右移**。但样本增量小，**结论不牢，下次继续看**。
  📌 口径注记：>2000ms 与 max 是**跨全部 hook 类型**统计的（3 条里有 1 条是 `stop`），
  与上一次巡检的算法一致 —— 预登记判据 2 只写 `ms_total`，未限定 hook。
- 文件完整性：`metrics.jsonl` 6877 行、0 解析失败、`ts` 严格单调 0 处逆序、**无 `.1` 轮转**。
- 窗口内 381 行，其中 `prompt_submit` 186 行。
- 方法同上次：读**活文件**、`ts >= 窗口起点` 过滤，**未算截尾率、未算 Wilson 区间、未打印 `n` 的分项**。
- 本次会话**未跑任何全量套件**（只读分析），因此**未产生新的需剔除的跑批窗口**。
- 🔴 **`n`（原始）已到 137，逼近 150 闸门。** 但闸门要求的是**剔除后**的 `n`（补遗 3），
  且读数必须先复制**冻结快照**。**在剔除后 `n ≥ 150` 之前，仍然只许报"数据不足"。**

**2026-08-13 23:19 那次的补充记录：**

- 🔴 **先记一次漏做**：上一条自己写着"08-12 当天应当在稍晚再做一次真正跨日的巡检"，
  **那次没做** —— 08-12 全天无巡检记录（当天 20:45 的会话只冻结了读数脚本）。
  本次是补做，**距上次巡检 46.6 小时**。预登记要求的是"窗口期内持续巡检"，**这里断过一次，如实记。**
- **判据 2 的成群检查（本轮重点，结论与上次不同）**：前 3 条**逐条不变**
  （`08-11 09:26:59 / 2112ms`、`08-11 21:16:03 / 2525ms（stop）`、`08-11 22:25:34 / 2864ms`），
  🆕 **新增第 4 条：`08-13 21:20:35 / 2265.73ms（prompt_submit）`**。**max 未变，仍是 2864ms。**
  ⇒ 读法：增量只有 1 条、跨了 46.6 小时、且**低于既有 max**，**仍不支持分布右移**；
  但**上次那句"逐条与上次相同、无新增"已经不成立** —— "单点离群"这个说法应弱化为
  **"高值稀疏、未见上移"**。**继续看，重点仍是有没有成群，不是有没有破 max。**
  📌 口径注记：>2000ms 与 max 一律**跨全部 hook 类型**统计（4 条里 1 条是 `stop`），与前两次算法一致。
- 文件完整性：`metrics.jsonl` **7003 行**、**0 解析失败**、`ts` 严格单调 **0 处逆序**、**无 `.1` 轮转**。
  最后一行 `08-13 23:17:30`。
- 窗口内 **507** 行，其中 `prompt_submit` **244** 行。窗口已开 **93.4 h**。
- 方法同前两次：读**活文件**、`ts >= 窗口起点` 过滤，**未算截尾率、未算 Wilson 区间、未算 p99、
  未打印 `n` 的分项**（打印分项等于提前读数）。
- **跑批窗口：自上次巡检（08-12 00:45）至今无新增需剔除的窗口，上面那张表不用动。**
  依据是**同期记录**：期间五次提交全是 docs-only（`docs(prereg)…` 之后的
  `docs(handoff): record the inspection mechanism…` / `docs(plan): log the 2026-08-12 00:45 inspection` /
  `docs(handoff): close P0#2's last block` / `docs: defuse the two exclusion landmines` /
  `docs(readout): freeze the Task 5 analysis script` / `update RULE 6` /
  `docs(handoff): point the entry page…`），且 handoff 顶部横幅当轮自记"**未跑任何全量套件**"。
  ⚠️ **效力边界照 Ⅺ.14 的写法明说**：这是**同期记录**，**不是直接观测**；"跑了套件但没提交"在原理上可能发生。
  **读数时若对某一小段存疑，正确处置是把该段一并剔除（保守方向），不是回忆当时干了什么。**
- 🔴🔴 **`n`（原始）= 176，已经越过 150 这个数值 —— 但这不等于闸门开了。**
  闸门要求的是**剔除后**的 `n`（补遗 3），而剔除后的 `n` **只有跑冻结脚本才算得出来，
  而那一跑同时就是读数**（脚本在 `n >= 150` 时会直接往下打印截尾率与区间）。
  ⇒ **顺序不能颠倒**：~~先裁决候选补遗 5 那两条 →~~ 再复制冻结快照、跑一次。
  ✅ **补遗 5 已于同日（2026-08-13）裁决完毕，脚本已按裁决改并重验** ⇒ **前置条件已清空。**
  📌 **本条写下时那两条还没裁**，同日晚些才裁完 —— 保留原文并划掉，免得下一个人以为还欠着。
  📌 **巡检阶段刻意不做剔除**（记录纪律见本节末），所以本行的 176 是**原始值**，不是闸门值。

**2026-08-19 00:08 那次的补充记录（补做，中间断档约 5 天）：**

- 🔴🔴 **先记断档**：上一次巡检是 **2026-08-13 23:19**，本次是 **2026-08-19 00:08**，
  **间隔 121.0 小时（约 5.0 天）**。预登记要求的是窗口期内**持续**巡检，
  **这一段是真的漏了**。⚠️ **不要把它读成"期间无事发生"——那是没测，不是测过了。**
  这是本窗口第 **二** 次断档（第一次是 08-12 的 46.6 小时），**且这次长了一倍有余。**
- **判据 1**：窗口内 `prompt_submit` 共 **281** 行，`error !== null` **0 命中 ✅**。
- **判据 2 的成群检查（本轮重点）**：>2000ms 仍是 **4 条，逐条与上次完全相同** ——
  `08-11 09:26:59 / 2111.66ms`、`08-11 21:16:03 / 2525.06ms（stop）`、
  `08-11 22:25:34 / 2864.04ms`、`08-13 21:20:35 / 2265.73ms`。**max 未变，仍是 2864.04ms。**
  ⇒ **断档期间新增 76 行（其中 `prompt_submit` 37 行），0 条新的 >2000ms。**
  跨 5 天、37 次真实提交零新增高值 ⇒ **"高值稀疏、未见上移"这个读法得到加强，仍不支持分布右移。**
  📌 口径注记：>2000ms 与 max 一律**跨全部 hook 类型**统计（4 条里 1 条是 `stop`），与前三次算法一致。
- **判据 3**：**不可操作化、未执行**（人类裁决 2026-08-10，补遗 2）。**不得默认它通过。**
- 文件完整性：`metrics.jsonl` **7079 行**、**0 解析失败**、`ts` 严格单调 **0 处逆序**、**无 `.1` 轮转**。
  最后一行 `08-19 00:06:03`（**就是本次会话自己产生的**，见下条）。
- 窗口内 **583** 行，其中 `prompt_submit` **281** 行。窗口已开 **214.2 h**。
- 方法同前三次：读**活文件**、`ts >= 窗口起点` 过滤，**未算截尾率、未算 Wilson 区间、未算 p99、
  未打印 `n` 的分项**。
  ✅ **本次脚本先做了自检**：把截止时刻卡回 `08-13 23:19` 重跑，
  **逐项复现了上一次的 507 / 244 / n=176 / 4 条 >2000ms / max 2864ms** ⇒ 口径与前三次一致，不是另发明的尺子。
- 🔴 **`n`（原始）= 200。** 仍是**原始值**，**不是闸门值** —— 闸门要**剔除后**的 `n`（补遗 3），
  而剔除后的 `n` **只有跑冻结脚本才算得出来，那一跑同时就是读数**。**原始 n 越过 150/200 都不等于闸门开了。**

### 🔴🔴 本次巡检撞出的新问题：断档期的批次剔除**没有同期记录可依**

前三次巡检都用「**期间提交全是 docs-only** ⇒ 没跑套件」来论证"无新增需剔除的窗口"。
**这个论证在本次断档期上不成立**，有两条独立的理由：

1. **本仓库在断档期确实零提交**（`d4c739f` 2026-08-14 00:21 之后直到 `7fbb3ba` 2026-08-19 00:04），
   所以"提交全是 docs-only"这句**退化成了"没有任何提交"** —— 它对"有没有跑套件"**零区分力**。
   零提交与"跑了套件但没提交"**完全不可分**。
2. 🔴 **更要命的一条：`metrics.jsonl` 是全局的**（`~/.claude/ccmem/metrics.jsonl`），
   **每行不带任何 project / cwd 归属字段**（实测 20 个字段里没有）。
   ⇒ 断档期的行**可能来自任何一个仓库的会话**，本仓库的 git 历史**在原理上就管不着它们**。
   而**别的仓库跑全量套件同样会抬本机负载** —— 剔除口径要挡的正是这个。

**断档期的逐日行数（只数行，不算任何结局量）**：

```
2026-08-14   rows  7   prompt_submit  3
2026-08-15   rows 22   prompt_submit 10
2026-08-16   rows 30   prompt_submit 16
2026-08-17   rows  0   prompt_submit  0     ← 全天零活动
2026-08-18   rows  8   prompt_submit  4
2026-08-19   rows  4   prompt_submit  2     ← 含本次会话自身
```

📌 **本条只做记录，不做裁决**（✅ **已于同日 2026-08-19 裁决，见下一小节「存疑段的裁决」——
结论是保留整段、原始 `n` 保持 200；本段以下的 (a)/(b) 保留原文，是提问时的原状，不是待办**）。
按本节既有的保守纪律
（「读数时若对某一小段存疑，正确处置是把该段一并剔除，不是回忆当时干了什么」），
**08-14 → 08-18 这一段在读数时属于"存疑段"**，处置有两条路，**都要人类点头，不许我自己选**：

- **(a) 保守剔除整段**：**实测代价是原始 `n` 从 200 掉到 178（-22）**。
  （📌 **不是 -33**：那 5 天有 33 行 `prompt_submit`，但其中只有 22 行是**真实 embed 尝试** ——
  `A` 的缓存命中与 `B-circuit`/`B-off` 不进分母，见 Ⅳ.23。**别拿行数当 `n`。**）
  ⚠️ 178 仍在 150 以上，但**这仍然是原始值** —— 窄义批次剔除还要再扣，闸门开不开**只有跑脚本才知道**。
- **(b) 保留整段**：需要人类以当事人身份确认那 5 天没在任何仓库跑过全量套件 ——
  **这是"回忆"，本节明文说过不算证据**，但人类的一手记忆与 agent 的事后推断不是一回事，由人类判定。

⚠️ **这条同时削弱了前三次巡检的同期记录论证**：那三次也是拿本仓库的提交去论证一个**全局文件**的清洁度。
断档期把这个缺口放大到看得见，但缺口**一直都在**。**读数报告里必须写出这一点。**

### 📌 2026-08-19 的测试运行记录（**不是全量套件，按窄义无需剔除，仍照纪律当场记**）

人类裁决：测试 A **写出来但不合并、不跑全量套件**。照此执行，**`main` 未动产品代码或测试**，
测试 A 落在分支 `config-value-parity`（**未合并**）。

**本次会话在本机跑过的测试，逐条如实记（都是单文件、亚秒级）：**

| 时刻 | 内容 | 时长 |
|---|---|---|
| 00:30:35 | `node --test tests/unit/v014-config-value-parity.test.mjs`（本仓库） | 47 ms |
| 00:3x | 同一文件，在 `git clone --local` 的**副本**里跑（变异验证，两次） | 各约 50 ms |
| 00:3x | 同一文件，本仓库，改完守卫后重跑 | 亚秒 |

⚠️ **口径说明**：窄义剔除针对的是**全量套件运行期**（本仓库 582 个测试，历史上单次约 17 秒）。
以上全是**单文件、总计不到 1 秒**的运行，**按补遗 3 的窄义不构成需剔除的跑批窗口**。
**但仍然记下来** —— 理由就是上面那条实证：本机负载来自哪里不可事后重建，**当场记比事后回忆强**。
📌 变异验证刻意放在 `git clone --local` 的副本里做，**真实 `config.default.json` 全程未被修改**
（`git diff` 为空，值仍是 `1200`）。

### ✅ 存疑段的裁决（2026-08-19，人类）：**保留整段**，但依据要如实写

> 🆕 **2026-08-20 更新（补遗 6）**：本节的裁决**仍然有效，且就是读数的主臂** ——
> 但它**不再是唯一的臂**。此后三次巡检三次抓到外来负载、并两次证明 `~/.zsh_history`
> 看不见 agent 驱动的跑批 ⇒ 本节所依据的两条证据都被削弱。
> **补遗 6 的处置：主臂保留（本节不变），另加一条强制的"再剔该段"敏感性臂，两臂一致才判。**
> **下文原样保留，是当时的裁决与依据，不是待办。**

**人类原话：「这 5 天我没怎么跑 claude」。**
⇒ 采纳 **(b) 保留整段**，`08-14…08-18` **不剔除**，原始 `n` 保持 **200**。

🔴 **但必须写清这句话是什么、不是什么**，否则读数报告会把它当成比实际更强的证据：

- 它是**人类一手记忆**，**不是同期观测**；且措辞是**「没怎么跑」（弱确认）**，
  **不是「完全没在任何仓库跑过全量套件」**。
- **提问与回答之间有口径差**：要剔的是**全量套件**（抬本机负载），
  而**跑全量套件根本不需要 claude** —— 一个终端里的 `npm test` 就够了。

**为把记忆换成观测，本次额外做了三项直接取证（结论有强有弱，逐条标明）：**

1. ✅ **强**：`~/.zsh_history` 是**扩展格式（带 epoch 时间戳）**，
   `08-14 00:00 → 08-19 00:00` 区间内 **交互式 shell 命令数 = 0**。
   ⚠️ **效力边界**：Claude Code 的 Bash 工具走非交互 shell，**不写 `~/.zsh_history`** ⇒
   这一条排除的是**人在终端里跑**，**排除不了 agent 驱动的跑批**。
2. ⚠️ **零效力，别拿它当证据**：`~/.npm/_logs/` 里**没有断档期的日志** ——
   但 `npm config get logs-max` = **10**，**旧日志会被自动裁掉**，
   现存 11 份**全部是 `2026-08-19 00:11–00:14` 的**（即断档之后）。
   ⇒ **"没有日志"是被裁掉了，不是没发生过。absence ≠ evidence。**
3. 🔴 **反向撞出的一条实证**：`2026-08-19 00:11–00:14`（**就在本次会话进行中**）
   本机跑了 `npm exec -- tsc -p tsconfig.json`。**那不是本仓库**
   （本仓库是 JS，**根本没有 `tsconfig.json`**，`package.json` name = `ccmem`），**也不是本次会话跑的**。
   ⇒ **这是"全局 `metrics.jsonl` 会吸收其他项目负载"的直接实例，而且就发生在窗口内、就在此刻。**
   **上面那个缺口不是理论担忧，是当场抓到的。**

📌 **合起来的读法**：断档期 `metrics` 只有 76 行 / 37 次 `prompt_submit`（5 天，很轻），
与「没怎么跑」相符；加上终端零命令 ⇒ **该段大概率干净，但没有被证明干净。**
⇒ **裁决是保留，理由是"证据倾向 + 人类确认"，不是"已排除"。**

🔴 **读数报告里必须原样写出这三条**（尤其第 3 条），
~~并说明**保守回退（整段剔除，原始 `n` 200 → 178）在读数时仍然可选** —— 一旦区间结果贴着 40.3%，
这个选择就会变成结论的一部分，**那时候再补论证就晚了。**~~

✅ **已于 2026-08-20 裁决，这条不再是待办 —— 见补遗 6。** 那句"读数时仍然可选"**已作废**：
**"可选"本身就是这条判据最后一个没冻结的自由度。** 现在的口径是
**双臂 + 一致才判**（主臂保留该段，敏感性臂再剔该段，两臂结论不一致就报"不可判定"），
**冻结脚本已按补遗 6 实现并用合成数据验过**。⇒ **读数时不许再选，也没得选。**
📌 上面这三条取证**仍然必须原样写进读数报告** —— 补遗 6 换掉的是"怎么处置"，**不是"要不要说"**。

### 🔴 巡检撞出的口径问题：`n` 到底数剔除前还是剔除后（✅ **已于 2026-08-12 裁决，见下方"裁决"**）

原始 `n = 114`。按天拆：

```
2026-08-10   n = 48    02:19:44 → 23:42:04
2026-08-11   n = 66    00:12:21 → 23:36:33
合计         n = 114
```

08-11 那 66 条几乎全部落在本会话跑 W4 的那一整天里，而本文档上一节当时写着
**「本会话 2026-08-11 全天活跃，按 Ⅰ 的口径整段属"曝露不明"」**
（📌 **那句已随本节的裁决作废，上一节现已改为窄义并把原文划掉** —— 此处引用的是发现问题时的原状）。

**问题**：预登记把 **`n ≥ 150 次真实 embed 尝试`** 写在「样本量」一节，
把 **「剔除本机跑批窗口」** 写在「分析口径」一节，**两者没有说清 `n` 是剔除前数还是剔除后数**。

而且「剔除跑批窗口」本身还有**窄义**（只剔全量套件运行期 + 60s 余量）与**整天义**
（Ⅰ 为探针定的"会话活跃即曝露不明"）两种读法，两者差别很大。

### ✅ 裁决（2026-08-12，人类采纳）

全文与理由见 **`…-prereg-addendum.md` 补遗 3**（**原预登记仍是零改动**）。结论两句：

> **① `n` 在剔除后的集合上数**（`n` 与截尾率必须同集合）。
> **② 「剔除跑批窗口」取窄义**：全量套件运行期 + 前后各至少 60s 余量；
> **不适用 Ⅰ 为探针定的"整天曝露不明"规则。**

关键理由：`prompt_submit` **按构造每个 prompt 一行 ⇒ 每一行都产生于活跃会话**，
整天义对它零区分力、照做等于剔光；而基线 `59/125` 本身就是按窄义算的（Ⅺ.3，只剔了 11 行）。

🔴 **更正一条本文档 2026-08-11 写错的数**：那一版写「按剔除后读手上最多 48」——
**那是按整天义算的，已随裁决作废**。按生效的窄义，剔除量只是那六个 17 秒窗口（+60s 余量）那一小撮，
**远不止 48**。确切值待读数时一次性算。

📌 **记录纪律（裁决的一部分）**：上表的 `n` **一律按"原始（未剔除）"记并显式标注**，
**不要在巡检阶段边记边剔** —— 窄义剔除依赖精确的批次起止时间（就在本文档上一节那张表里），
放到读数时做一次，免得每天的数依赖当天的记忆。

---

**2026-08-19 23:31 那次的补充记录（第 5 次，距上次 23.4 小时）：**

- ✅ **本窗口第一次真正"每日"的跨日巡检，没有断档。** 前两次断档分别是 46.6 h 与 121.0 h，
  这次是 **23.4 h**（上次 `2026-08-19 00:08`）。**节奏回来了，别再让它断。**
- **判据 1**：窗口内 `prompt_submit` 共 **306** 行，`error !== null` **0 命中 ✅**。
- **判据 2 的成群检查（本轮重点）**：>2000ms 仍是 **4 条，逐条与上次完全相同** ——
  `08-11 09:26:59 / 2111.66ms`、`08-11 21:16:03 / 2525.06ms（stop）`、
  `08-11 22:25:34 / 2864.04ms`、`08-13 21:20:35 / 2265.73ms`。**max 未变，仍是 2864.04ms。**
  ⇒ **自上次巡检新增 50 行（其中 `prompt_submit` 25 行），0 条新的 >2000ms。**
  ⇒ **"高值稀疏、未见上移"继续成立，仍不支持分布右移。**
  📌 口径注记：>2000ms 与 max 一律**跨全部 hook 类型**统计（4 条里 1 条是 `stop`），与前四次算法一致。
- **判据 3**：**不可操作化、未执行**（人类裁决 2026-08-10，补遗 2）。**不得默认它通过。**
- 文件完整性：`metrics.jsonl` **7129 行**、**0 解析失败**、`ts` 严格单调 **0 处逆序**、
  **无 `.1` 轮转**（`~/.claude/ccmem/` 下只有 `metrics.jsonl` 一份）。最后一行 `08-19 23:30:14`。
- 窗口内 **633** 行，其中 `prompt_submit` **306** 行。窗口已开 **237.6 h**。
- 方法同前四次：读**活文件**、`ts >= 窗口起点` 过滤，**未算截尾率、未算 Wilson 区间、未算 p99、
  未打印 `n` 的分项**。
  ✅ **脚本自检**：把截止时刻卡回 `2026-08-19 00:08` 重跑，
  **逐项复现了上一次的 583 / 281 / n=200 / 4 条 >2000ms / max 2864.040875ms**
  ⇒ 与前四次是同一把尺子，不是另发明的。
- 🔴 **`n`（原始）= 214。** 仍是**原始值**，**不是闸门值** —— 闸门要**剔除后**的 `n`（补遗 3），
  而剔除后的 `n` **只有跑冻结脚本才算得出来，那一跑同时就是读数**。
  **原始 `n` 越过 150 / 200 / 214 都不等于闸门开了。**
- 📌 **活文件的漂移这次当场量到了**：巡检脚本跑完那一刻（`23:31:49`）文件是 **7129 行**，
  几分钟后再读已经是 **7130 行**。**这就是预登记要求 Task 5 必须先复制冻结快照的原因**，别省这一步。

### 🔴🔴 又抓到一次跨项目负载 —— 比上次那条 `tsc` 重得多，而且**与本次巡检同时发生**

上一次巡检撞出的那条（`metrics.jsonl` 是**全局文件**、每行**不带任何 project / cwd 归属**）本轮**再次被实证**，
而且这一例比 `tsc` 那例强：

- **时刻**：`2026-08-19 23:30:15 → 23:32:21`，**就在本次巡检进行中**，**仍在测量窗口内**。
- **内容**：`~/.npm/_logs/` 现存 11 份日志**全部**落在这个区间，逐条都是
  `npm exec --yes --package @playwright/cli -- playwright-cli …`（`open` / `eval` / `resize`），
  **cwd 全部是 `/Users/biran/project/yan/HFUT_devmgmt_system`** —— **不是本仓库**。
- **为什么比 `tsc` 那例重**：它不是一次性编译，而是**驱动一个真实浏览器**
  （`playwright-cli open http://localhost:3000/dashboard` ⇒ 本机另有一个 dev server 在跑），
  且 `npm exec --yes` 每次都要过 registry。**这是持续的、多进程的本机负载。**
- **同期性**：本窗口 `23:00` 那一小时产生了 **10 次 `prompt_submit`**，**与这段负载时间重叠**。
  ⇒ **"窗口内的行在外来负载下产生"不再是原理上的可能，是当场量到的第二例。**

⚠️ **它不构成窄义剔除的触发条件** —— 补遗 3 的窄义只剔**本仓库全量套件运行期**（+ 前后各 ≥60s），
**所以本次巡检不新增需剔除的批次窗口，上面那张批次表不用动。**
🔴 **但读数报告必须把它与 `08-19 00:11` 那条 `tsc` 一起写出来**：两条指向的是同一件事 ——
**"本仓库提交全是 docs-only ⇒ 没跑套件"这个论证，对一个全局文件永远不成立**，
而且**外来负载不是罕见事件，两次巡检两次撞到**。

**同期取证（照 Ⅺ.14 的写法逐条标效力）：**

1. ✅ **强**：`~/.zsh_history`（扩展格式、带 epoch）在 `08-19 00:08 → 23:31` 区间内
   **只有 1 条交互式命令**：`22:49:50 claude`。**没有任何人在终端里敲过 `npm test`。**
   ⚠️ 效力边界不变：**排除不了 agent 驱动的跑批**。
   🔴 **本轮正好给了这条边界一个实证**：上面那 11 条 playwright **一条都没进 `~/.zsh_history`**。
2. ⚠️ **`~/.npm/_logs/` 这次给出的是正向证据**（有日志 ⇒ 确实发生过），**这个方向可用**；
   但**反向仍然零效力**（`logs-max = 10`，"没有日志"只说明被裁掉了）。
   **别因为这次能用就把上次那条"absence ≠ evidence"翻过来。**
3. ✅ **本会话直接观测**：本会话**未跑任何测试**（全量套件与单文件都没有），
   只做只读分析与两处文档编辑 ⇒ **本会话未产生需剔除的跑批窗口。**
   ⚠️ **但 `00:08 → 22:49` 这 22.7 小时不在本会话的观测范围内**，只有上面两条间接证据托着。

---

**2026-08-20 23:05 那次的补充记录（第 6 次，距上次 23.6 小时）：**

- ✅ **连续第二次守住每日节奏**（上次 `2026-08-19 23:31`，间隔 **23.6 h**）。
  本窗口的断档史：46.6 h（08-12）、121.0 h（08-14…08-18），此后 23.4 h、23.6 h。**别再让它断。**
- **判据 1**：窗口内 `prompt_submit` 共 **320** 行，`error !== null` **0 命中 ✅**。
- **判据 2 的成群检查（本轮重点）**：>2000ms 仍是 **4 条，逐条与上次完全相同** ——
  `08-11 09:26:59 / 2111.663541ms`、`08-11 21:16:03 / 2525.063584ms（stop）`、
  `08-11 22:25:34 / 2864.040875ms`、`08-13 21:20:35 / 2265.728667ms`。**max 未变，仍是 2864.040875ms。**
  ⇒ **自上次巡检新增 33 行（其中 `prompt_submit` 14 行），0 条新的 >2000ms。**
  ⇒ 这是自 `08-13 21:20:35` 那条之后**连续第 3 次零新增**（08-19 00:08、08-19 23:31、本次），
  跨 **7.1 天**、**76 次 `prompt_submit`** 零新增高值 ⇒ **"高值稀疏、未见上移"继续成立，仍不支持分布右移。**
  📌 口径注记：>2000ms 与 max 一律**跨全部 hook 类型**统计（4 条里 1 条是 `stop`），与前五次算法一致。
  🆕 **增量内的 max 是 `08-20 22:28:48 / 1832.127ms`（`prompt_submit`）** ——
  仍在 2000ms 以下，**不进上面那张 4 条的名单**。
  ⚠️ **这条是同尺子的补充读数（同一个 `ms_total`，只把范围缩到增量），不是新判据**，
  记它的唯一理由是"成群"要看的是**有没有上移**，而"0 条 >2000ms"这句话本身看不出增量里最高摸到哪。
  📌 **它与本轮观测到的外来负载没有时间重叠**（那些负载在 22:39 之后，见下），**别顺手把两者归因到一起。**
- **判据 3**：**不可操作化、未执行**（人类裁决 2026-08-10，补遗 2）。**不得默认它通过。**
- 文件完整性：`metrics.jsonl` **7162 行**、**0 解析失败**、`ts` 严格单调 **0 处逆序**、
  **无 `.1` 轮转**（`~/.claude/ccmem/` 下只有 `metrics.jsonl` 一份）。最后一行 `08-20 23:02:50`
  （**是本次会话自己产生的**）。
- 窗口内 **666** 行，其中 `prompt_submit` **320** 行。窗口已开 **261.2 h**。
- 方法同前五次：读**活文件**、`ts >= 窗口起点`（`2026-08-10T01:53:30+08:00`）过滤，
  **未算截尾率、未算 Wilson 区间、未算 p99、未打印 `n` 的分项**。
  ✅ **脚本自检**：把截止时刻卡回 `2026-08-19T23:31:49+08:00` 重跑，
  **逐项复现了上一次的 633 / 306 / n=214 / 4 条 >2000ms / max 2864.040875**
  ⇒ 与前五次是同一把尺子，不是另发明的。
- 🔴 **`n`（原始）= 222。** 仍是**原始值**，**不是闸门值** —— 闸门要**剔除后**的 `n`（补遗 3），
  而剔除后的 `n` **只有跑冻结脚本才算得出来，那一跑同时就是读数**。
  **原始 `n` 越过 150 / 200 / 214 / 222 都不等于闸门开了。**

### 🔴🔴 第三个跨项目负载实例 —— 这次是**别的仓库在跑测试套件**（前两次是 `tsc` 与浏览器）

ⅩⅤ.2 / ⅩⅥ.2 那条（`metrics.jsonl` 是**全局文件**、每行**不带任何 project / cwd 归属**）
**本轮第三次被实证，而且这一例最贴近剔除口径要挡的东西：它就是测试套件运行。**

- **时刻**：`2026-08-20 22:39:56`、`22:40:20`、`22:40:35`（本地时区），**在测量窗口内**。
- **内容**：`~/.npm/_logs/` 里三份日志的 argv 分别是
  `npm exec -- vitest run tests/unlock/unlockCommand.test.ts`、
  `… tests/unlock/__probe_close_hazard.test.ts`、`… tests/unlock/__probe_close_hazard2.test.ts`，
  **cwd 全部是**
  `/private/tmp/claude-501/-Users-biran-code-skills-loop-ccloop/…/scratchpad/mutant-e1b`
  —— **不是本仓库**，是 `skills/loop-ccloop` 的一个 **变异体（`mutant-*`）副本**在跑 vitest。
- **同一区间还有另外两类本机负载**（同样逐份读了 argv 与 cwd）：
  `22:59:19` 与 `23:02:54` 两次 `npm install --global @anthropic-ai/claude-code@2.1.237`（cwd `/Users/biran`）；
  `23:02:45`/`23:02:48` 两次 `npm exec --yes -- chrome-devtools-mcp@latest`
  —— 后者 cwd 是**本仓库**，是**本次会话自己**启动 MCP server 带出来的。
- **为什么这一例最重**：前两例（`tsc` 编译、playwright 驱浏览器）还能argue"不是测试"；
  **这一例是货真价实的 `vitest run`**，正是补遗 3 窄义剔除想挡的那类负载 ——
  只不过窄义只剔**本仓库全量套件运行期**，所以**它不触发剔除，批次表不动**。
  ⇒ **"跑批窗口"这个概念本身就漏**：它按仓库划，而负载按机器发生。

⚠️ **不构成窄义剔除的触发条件** ⇒ **本次巡检不新增需剔除的批次窗口，批次表不用动。**
🔴 **读数报告必须把三条一起写**（`08-19 00:11` 的 `tsc`、`08-19 23:30` 的 playwright、
本次 `08-20 22:39` 的 vitest）：**三次巡检三次抓到，外来负载不是罕见事件，是常态。**

**同期取证（照 Ⅺ.14 的写法逐条标效力）：**

1. ✅ **强（正向）**：`~/.npm/_logs/` 逐份读了 argv 与 cwd ⇒ 上述负载是**直接观测**，不是推断。
2. ✅ **强**：`~/.zsh_history`（扩展格式、带 epoch）在 `08-19 23:31 → 08-20 23:05` 区间内
   **交互式命令数 = 0**。⇒ **没有人在终端里敲过任何命令。**
   🔴 **本轮又给这条边界补了一个实证**：上面那三次 `vitest run` **一条都没进 `~/.zsh_history`**
   ⇒ **zsh_history 排除不了 agent 驱动的跑批**，这已经是第二次当场证明。
3. 🔴🔴 **本轮自己弄坏了一份证据，如实记**：为查 `logs-max` 跑的那条 `npm config get logs-max`
   **本身又写了一份 npm 日志**，把当时最旧的那份（`2026-08-20T14_39_31Z`）**挤掉了**
   （`logs-max = 10`，实测目录里现存 11 份 = 10 + 当前）。
   ⇒ 那一份大概率是同一批 vitest 里的第 1 次，**内容已不可读**。
   **教训：`~/.npm/_logs/` 是个会被自己的观测动作污染的证据源；下次要先 `cp -r` 再查。**
   ⚠️ 并且这让"**absence ≠ evidence**"更硬了一层：现在**连观测者自己都会制造 absence**。
4. ✅ **本会话直接观测**：本会话**未跑任何测试**（全量套件与单文件都没有），
   只做只读分析与文档编辑 ⇒ **本会话未产生需剔除的跑批窗口。**
   ⚠️ **但 `08-19 23:31 → 08-20 22:28` 这 23 小时不在本会话的观测范围内**，只有上面两条间接证据托着。

---

### 🔴 2026-08-21 补记：上面三次取证里的 `~/.zsh_history` 结论全部降级（**口径不变，只是证据变弱**）

前三次巡检的同期取证都把 `~/.zsh_history` 标成 **✅ 强**，措辞是
"**没有人在终端里敲过任何命令**"。**这句话现在被直接推翻**：本仓库
`.git/logs/refs/remotes/origin/main` 显示，**那三个取证区间各自都套着至少一次人类的 `git push`，
而它们一条都没进 `~/.zsh_history`**：

| 取证区间 | 原记录里的 zsh_history 结论 | 区间内实际发生的人类 push |
|---|---|---|
| `08-14 00:00 → 08-19 00:00`（**断档段**） | 交互式命令数 **= 0** | `08-14 00:14:35` |
| `08-19 00:08 → 23:31` | **只有 1 条**（`22:49:50 claude`） | `08-19 22:48:50`、`08-19 23:30:04` |
| `08-19 23:31 → 08-20 23:05` | 交互式命令数 **= 0** | `08-20 00:33:36` |

**源头**：**人类 2026-08-21 亲口确认是自己推的，用 Warp / GUI / 别的终端** ——
那些入口**不写 `~/.zsh_history`**。排查过程与全部反证（不是 Claude 会话推的、不是钩子推的、
ccmem 与 `loop/ccloop` 两个仓库 `08-20 23:06:2x` 相差 4 秒被同批推）见 handoff **ⅩⅦ.7**。

⇒ **正确措辞**：`~/.zsh_history` 只能说"**没有人在写它的那个 shell 里敲过命令**"。
盲区已知三类：① agent 驱动的跑批（两次实证）② 其他项目的 `npm exec`（三次实证）
③ 🆕 **人类用别的入口敲的命令（三次实证）**。

⚠️ **别把这条读大**：`git push` **不是全量套件运行**，**不触发补遗 3 的窄义剔除** ⇒
**上面那张批次表不动、原始 `n` 不动、补遗 6 的双臂口径不动。本条不解冻任何已冻结的口径。**
🔴 **但读数报告必须照上表把那三条 ✅强 降级重写**，与三条跨项目负载实例并列。
📌 **以后的巡检**：`~/.zsh_history` 仍可记，但**只能标 ⚠️ 弱**，且要同时看
`.git/logs/refs/remotes/origin/main` —— 那是目前唯一能看见"人类在本机干过事"的证据源。

---

**2026-08-23 10:02 那次的补充记录（第 7 次，🔴 距上次 58.9 小时 —— 本窗口第三次断档）：**

- 🔴🔴 **先记断档，并且这次成因是清楚的、还被自己预告过**：上一次巡检 `2026-08-20 23:05`，
  本次 `2026-08-23 10:02`，**间隔 58.9 小时**。本窗口断档史：**46.6 h（08-12）、121.0 h（08-14…08-18）、
  58.9 h（本次）**，此前刚守住两次 23.4 h / 23.6 h。
  **成因**：`08-21 00:28` 那次会话开工时距上次巡检只有 1.4 h，按人类裁决选了"**今天晚些（≥20:00）再做**"，
  会话 `00:43` 结束；**08-21 与 08-22 全天没有会话** ⇒ **那次巡检从未发生**。
  ⚠️ **同一个失败模式在本窗口已经出现两次**（08-12 写着"当天稍晚再做"、08-21 裁决"今天晚些做"，两次都没做成）。
  🔴 **教训写死在这里：口头安排"稍晚再做"在本项目上的成功率是 0/2 —— 要么当场做，要么排一个真的定时器。**
  ⚠️ **别把断档读成"期间无事发生"：那是没测，不是测过了。**
- **判据 1**：窗口内 `prompt_submit` 共 **365** 行，`error !== null` **0 命中 ✅**。
- **判据 2 的成群检查（本轮重点）**：>2000ms 仍是 **4 条，逐条与上次完全相同** ——
  `08-11 09:26:59 / 2111.663541ms`、`08-11 21:16:03 / 2525.063584ms（stop）`、
  `08-11 22:25:34 / 2864.040875ms`、`08-13 21:20:35 / 2265.728667ms`。**max 未变，仍是 2864.040875ms。**
  ⇒ **自上次巡检新增 94 行（其中 `prompt_submit` 45 行），0 条新的 >2000ms。**
  ⇒ 这是自 `08-13 21:20:35` 那条之后**连续第 4 次零新增**（08-19 00:08、08-19 23:31、08-20 23:05、本次），
  跨 **9.5 天**、**121 次 `prompt_submit`** 零新增高值 ⇒ **"高值稀疏、未见上移"继续成立，仍不支持分布右移。**
  📌 口径注记：>2000ms 与 max 一律**跨全部 hook 类型**统计（4 条里 1 条是 `stop`），与前六次算法一致。
  🆕 **增量内的 max 是 `08-22 00:06:24 / 1593.209208ms`（`prompt_submit`）** ——
  比上一轮增量的 1832ms **更低**，仍远在 2000ms 以下、不进上面那张 4 条的名单。
  ⚠️ 同上轮：这是**同尺子缩到增量的补充读数，不是新判据**。
- **判据 3**：**不可操作化、未执行**（人类裁决 2026-08-10，补遗 2）。**不得默认它通过。**
- 文件完整性：`metrics.jsonl` **7256 行**、**0 解析失败**、`ts` 严格单调 **0 处逆序**、
  **无 `.1` 轮转**（`~/.claude/ccmem/` 下只有 `metrics.jsonl` 一份）。最后一行 `08-23 09:39:55`。
- 窗口内 **760** 行，其中 `prompt_submit` **365** 行。窗口已开 **320.1 h**。
- 方法同前六次：读**活文件**、`ts >= 窗口起点`（`2026-08-10T01:53:30+08:00`）过滤，
  **未算截尾率、未算 Wilson 区间、未算 p99、未打印 `n` 的分项**。
  ✅ **这次做了两道自检**（前几次只做一道）：截止时刻卡回 `2026-08-19T23:31:49+08:00`
  逐项复现 `7129 / 633 / 306 / n=214 / 4 条 / max 2864.040875`；再卡回 `2026-08-20T23:05:30+08:00`
  逐项复现 `7162 / 666 / 320 / n=222 / 4 条`，**连增量 max `1832.127ms @ 08-20 22:28:48` 都逐位吻合**
  ⇒ 与前六次是同一把尺子。
- 🔴 **`n`（原始）= 248。** 仍是**原始值**，**不是闸门值** —— 闸门要**剔除后**的 `n`（补遗 3），
  而剔除后的 `n` **只有跑冻结脚本才算得出来，那一跑同时就是读数**。
  **原始 `n` 越过 150 / 200 / 214 / 222 / 248 都不等于闸门开了。**

### 🔴🔴 第四个跨项目负载实例 —— 规模最大，且**就发生在本次巡检前 1 分钟**

- **时刻**：`2026-08-23 10:01:25 → 10:01:59`（本地时区），**在测量窗口内**，
  **比本次巡检脚本开跑（10:02:07）早不到一分钟**。
- **内容**：`~/.npm/_logs/` 快照里 11 份日志逐份读了 argv 与 cwd ——
  **34 秒内 10 次 `npm test`**：9 次 cwd 在
  `/private/var/folders/.../ccloop-run-scenario-*/{fixture,worktrees/attempt-1,runs/A-01/...}`
  （`loop/ccloop` 的场景临时目录），1 次 cwd 是 `/Users/biran/code/skills/loop/ccloop` 本身
  （`npm test -- --run tests/persistence/fileStore.test.ts`），外加 `10:01:59` 一次 `npm run dev`。
  **不是本仓库。**
- **为什么这一例最重**：上一例（08-20 的 3 次 `vitest run`）已经是测试套件，**这一例是它的 3 倍多、并发跑
  9 个 fixture，而且与本次读数几乎同刻** ⇒ **"读数当刻本机是干净的"这个假设，本轮当场被证伪。**
- ⇒ **四次巡检四次抓到外来负载。它不是罕见事件，是常态。**

⚠️ **不构成窄义剔除的触发条件**（窄义只剔**本仓库全量套件运行期**）⇒ **批次表不动。**
⚠️ **效力边界（比前几轮更硬）**：`logs-max = 10` ⇒ 这份快照只覆盖 **34 秒**；
**58.9 小时断档期里的 npm 活动一份都读不到** —— **absence ≠ evidence，那段仍然是"没观测"。**
✅ **上一轮的自伤教训已执行**：本次**先 `cp -r ~/.npm/_logs` 成快照再逐份读**，未污染证据源。

**同期取证（照 Ⅺ.14 的写法逐条标效力）：**

1. ✅ **强（正向）**：`~/.npm/_logs/` 快照逐份读了 argv 与 cwd ⇒ 上述负载是**直接观测**。
2. ⚠️ **弱 —— 本轮起正式降级，理由见上一节 2026-08-21 补记**：`~/.zsh_history` 在
   `08-20 23:05 → 08-23 10:02` 内只有 **3 条**（`08-20 23:17` 三次 `ggs` = `git status`）。
   🔴 **而同期确实发生过 3 次 `git push`** —— ccmem `08-21 00:25:01`、`08-21 00:43:23`，
   ccloop `08-22 11:07:27` —— **一条都没进 `~/.zsh_history`**。
   ⇒ **这是该盲区的第四次实证**，本条**不得再标 ✅强**。
3. ✅ **会话直接观测**：本会话（08-23）与上一次会话（08-21 00:28–00:43）**都未跑任何测试**
   （全量套件与单文件都没有），只做只读分析与文档编辑 ⇒ **本仓库未产生需剔除的跑批窗口。**
   ⚠️ **但 `08-21 00:43 → 08-23 10:00` 这 57 小时不在任何会话的观测范围内。**
4. ⚠️ **零效力，仅作记录**：本仓库在区间内只有 1 个提交（`138242e`，docs-only，`08-21 00:43`）。
   **"提交全是 docs-only ⇒ 没跑套件"这个论证对全局文件不成立**（ⅩⅤ.2），**不要拿它当证据。**

📌 **本次会话在本机做过的动作，照纪律当场记（都不构成需剔除的跑批窗口）**：

| 时刻 | 内容 | 性质 |
|---|---|---|
| 10:02:07 | 巡检脚本（scratchpad 里的只读 `.mjs`，跑了 3 次：两道自检 + 正式一次） | 只读，亚秒 |
| ~10:02 | `cp -r ~/.npm/_logs` 成快照 | 只读复制 |
| 10:42:43 | `launchctl bootstrap` + `kickstart` 装并实测每日提醒 `local.ccmem-inspect-reminder` | 一次 `osascript`，亚秒 |

⚠️ 以上全是**亚秒级、非测试**的动作，**按补遗 3 的窄义不构成需剔除的跑批窗口**（窄义只剔本仓库全量套件）。
**仍然记下来** —— 理由同 2026-08-19 那条：本机负载来自哪里**不可事后重建，当场记比事后回忆强**。
📌 **未跑任何测试**（全量套件与单文件都没有），**未跑冻结读数脚本**。


---

**2026-08-25 00:02 那次的补充记录（第 8 次，🔴 距上次 38.0 小时 —— 本窗口第四次断档）：**

- 🔴 **先记断档**：上一次巡检 `2026-08-23 10:02:07`，本次 `2026-08-25 00:02:16`，**间隔 38.0 小时**。
  本窗口断档史：**46.6 h（08-12）、121.0 h（08-14…08-18）、58.9 h（08-23）、38.0 h（本次）**，
  中间只守住过两次 23.4 h / 23.6 h。
  **成因与前两次不同**：08-23、08-24 两天没有会话 —— **不是"推到稍晚"，是没人开工**。
  ⚠️ 这正是 ⅩⅧ.7 那两层定时器要挡的失败模式，而**第 1 层（会话内 cron）随 08-23 那个会话一起死了**
  ⇒ 08-24 全天没有任何自动触发；**第 2 层（launchd）如期响了**（`~/.ccmem-inspect-reminder.log`
  有 `2026-08-24 21:00:38 reminder fired`），**但人类没看见那条通知**（本轮当面确认，见下方"未闭合"处置）。
  ⇒ **两层机制本轮各暴露了一个真实缺口，都已处置**，见本节末与 handoff ⅩⅨ。
  ⚠️ **别把断档读成"期间无事发生"：那是没测，不是测过了。**
- **判据 1**：窗口内 `prompt_submit` 共 **388** 行，`error !== null` **0 命中 ✅**。
- **判据 2 的成群检查（本轮重点）**：>2000ms 仍是 **4 条，逐条与上次完全相同** ——
  `08-11 09:26:59 / 2111.663541ms`、`08-11 21:16:03 / 2525.063584ms（stop）`、
  `08-11 22:25:34 / 2864.040875ms`、`08-13 21:20:35 / 2265.728667ms`。**max 未变，仍是 2864.040875ms。**
  ⇒ **自上次巡检新增 47 行（其中 `prompt_submit` 23 行），0 条新的 >2000ms。**
  ⇒ 这是自 `08-13 21:20:35` 那条之后**连续第 5 次零新增**
  （08-19 00:08、08-19 23:31、08-20 23:05、08-23 10:02、本次），
  跨 **11.0 天**、**144 次 `prompt_submit`** 零新增高值
  ⇒ **"高值稀疏、未见上移"继续成立，仍不支持分布右移。**
  📌 口径注记：>2000ms 与 max 一律**跨全部 hook 类型**统计（4 条里 1 条是 `stop`），与前七次算法一致。
  🆕 **增量内的 max 是 `08-24 01:29:40 / 1558.315417ms`（`prompt_submit`）** ——
  比上一轮增量的 1593ms **再低一点**，仍远在 2000ms 以下、不进上面那张 4 条的名单。
  ⚠️ 同前两轮：这是**同尺子缩到增量的补充读数，不是新判据**。
- **判据 3**：**不可操作化、未执行**（人类裁决 2026-08-10，补遗 2）。**不得默认它通过。**
- 文件完整性：`metrics.jsonl` **7303 行**、**0 解析失败**、`ts` 严格单调 **0 处逆序**、
  **无 `.1` 轮转**（`~/.claude/ccmem/` 下只有 `metrics.jsonl` 一份）。最后一行 `08-25 00:01:23`
  （**是本次会话自己产生的**）。
- 窗口内 **807** 行，其中 `prompt_submit` **388** 行。窗口已开 **358.1 h**。
- 方法同前七次：读**活文件**、`ts >= 窗口起点`（`2026-08-10T01:53:30+08:00`）过滤，
  **未算截尾率、未算 Wilson 区间、未算 p99、未打印 `n` 的分项**。
  ✅ **脚本是上一轮那个只读脚本原样复制，一行未改**（cutoff 走命令行参数）；
  截止时刻卡回 `2026-08-23T10:02:07+08:00`，**逐项复现了
  `7256 / 760 / 365 / n=248 / 4 条 / max 2864.040875 / 增量 94 行·45 ps·max 1593.209208`**
  ⇒ 与前七次是同一把尺子。
- 🔴 **`n`（原始）= 263。** 仍是**原始值**，**不是闸门值** —— 闸门要**剔除后**的 `n`（补遗 3），
  而剔除后的 `n` **只有跑冻结脚本才算得出来，那一跑同时就是读数**。
  **原始 `n` 越过 150 / 200 / 248 / 263 都不等于闸门开了。**

### 🔴🔴 第五个跨项目负载实例 —— 与本会话开工**同一秒**

- **时刻**：`2026-08-24 23:19:46 → 23:20:03`（本地时区），**在测量窗口内**，
  距本次巡检脚本开跑（`00:02:16`）约 **42 分钟**；
  🔴 **其中第一条 `23:19:46` 与本会话的第一条命令（`23:19:32`）相差 14 秒。**
- **内容**：`~/.npm/_logs/` 快照 11 份日志逐份读了 argv 与 cwd ——
  **5 次 `npm test`**，cwd 全在
  `/private/var/folders/.../ccloop-run-scenario-{mismatch,taskid-mismatch,not-observable}-*/`
  的 `fixture-a` / `fixture-b` / `fixture` / `run/worktrees/attempt-1` 里（`loop/ccloop` 的场景临时目录）；
  随后 `23:20:01` 一次 `npm run typecheck`、`23:20:03` 一次 `npm run build`，
  cwd 均为 `/Users/biran/code/skills/loop/ccloop`。**不是本仓库。**
- 另有 4 份是 `npm view @anthropic-ai/claude-code@latest version --prefer-online`
  （`23:47`、`23:59`、`00:00`、`00:01`，cwd `/Users/biran`）—— **是 harness 自己查版本，不是负载**，
  但**它证明这个证据源确实会被本会话自己的动作挤占**（上一轮那条自伤教训的同一机制）。
- ⇒ **五次巡检五次抓到外来负载。它不是罕见事件，是常态。**
- 📌 **同一个来源第三次出现**（`loop/ccloop`：08-20 的 3 次 `vitest run`、08-23 的 10 次 `npm test`、
  本次 5 次 `npm test` + typecheck + build）⇒ **跨项目负载的主要来源是 `loop/ccloop`，不是随机的。**

⚠️ **不构成窄义剔除的触发条件**（窄义只剔**本仓库全量套件运行期**）⇒ **批次表不动、原始 `n` 不动、
补遗 6 的双臂口径不动。**
⚠️ **效力边界**：快照 11 份只覆盖 `23:19:46 → 00:01:15` 共约 **42 分钟**；
**38.0 小时断档期里的 npm 活动一份都读不到** —— **absence ≠ evidence，那段仍然是"没观测"。**
✅ **自伤教训继续执行**：本次**先 `cp -r ~/.npm/_logs` 成快照再逐份读**；
且**未跑 `npm config get logs-max`**（上一轮正是它挤掉了最旧一份），
改为读 `~/.npmrc`（未显式设置）—— **观测动作本身不再污染证据源。**

**同期取证（照 Ⅺ.14 的写法逐条标效力）：**

1. ✅ **强（正向）**：`~/.npm/_logs/` 快照逐份读了 argv 与 cwd ⇒ 上述负载是**直接观测**。
2. ⚠️ **弱（承 2026-08-21 的降级，本轮第五次实证）**：`~/.zsh_history` 在
   `08-23 10:02 → 08-25 00:02` 内**一条都没有**。
   🔴 **而同期确实发生过 1 次 `git push`** —— ccloop `08-23 12:20:06`
   （`.git/logs/refs/remotes/origin/main`）—— **没进 `~/.zsh_history`**。
   ⇒ **第五次实证，本条不得标 ✅强。**
3. ✅ **强（负向，reflog）**：**ccmem 在区间内 0 次 push** —— `origin/main` 最后一次仍是
   `08-21 00:43:23`（`138242e`）。⇒ 本轮开工时 `git status -sb` 显示 `ahead 3` 与此一致。
4. ✅ **会话直接观测**：本会话（08-24/25）**未跑任何测试**（全量套件与单文件都没有），
   只做只读分析、文档编辑与定时器维护 ⇒ **本仓库未产生需剔除的跑批窗口。**
   ⚠️ **但 `08-23 10:42 → 08-24 23:19` 这 36.6 小时不在任何会话的观测范围内。**
5. ⚠️ **零效力，仅作记录**：本仓库在区间内**零提交**。
   **"提交全是 docs-only ⇒ 没跑套件"这个论证对全局文件不成立**（ⅩⅤ.2），**不要拿它当证据。**

📌 **本次会话在本机做过的动作，照纪律当场记（都不构成需剔除的跑批窗口）**：

| 时刻 | 内容 | 性质 |
|---|---|---|
| 23:19–23:20 | `git status` / `git branch` / `git worktree list` / `sed` 读文档 | 只读，亚秒 |
| ~00:02 | `cp -r ~/.npm/_logs` 成快照 | 只读复制 |
| 00:02:16 | 巡检脚本（scratchpad 里的只读 `.mjs`，跑了 2 次：一道自检 + 正式一次） | 只读，亚秒 |
| 00:0x | 读 `~/.zsh_history` / 两个仓库的 `origin/main` reflog | 只读，亚秒 |

⚠️ 以上全是**亚秒级、非测试**的动作，**按补遗 3 的窄义不构成需剔除的跑批窗口**。
**仍然记下来** —— 理由同前：本机负载来自哪里**不可事后重建，当场记比事后回忆强**。
📌 **未跑任何测试**（全量套件与单文件都没有），**未跑冻结读数脚本 `…-readout.mjs`**。

---

**2026-08-25 21:11 那次的补充记录（第 9 次，间隔 21.2 h）：**

- ~~🆕✅ **本窗口第一次由定时器而非人的记性触发的巡检 —— 两层机制端到端跑通了一次。**
  链路逐环可查：launchd 提醒 `20:54:51 reminder fired` → 人类 `21:03:12 ACK`（点掉了对话框）
  → 会话内 cron 到点触发 → `<20h` 守卫放行（21.2 h ≥ 20 h）→ 当场做。~~
  🔴🔴 **这段是错的，人类当轮当场更正（2026-08-25 21:2x）。原文保留划掉，免得下一位照抄。**
  **真实链路是**：launchd 提醒 `20:54:51` 弹出对话框 → 人类 `21:03:12` 点掉 →
  **人类由此想起第 1 层 cron 一直没动静，手工把 cron 的 prompt 重发了一遍** → 巡检才发生。
  🔴 **第 1 层没有产出这次巡检。产出它的是第 2 层加上一个人。**
  **我把"人手工重发的 prompt"误当成了"cron 自己到点触发"** —— 两者送到我面前长得一模一样，
  **我没有任何本地证据能分辨它们，却写下了分辨过的结论。** 这正是横幅第一条铁律说的静默失败的镜像：
  **不是把失败读成成功，是把"人做的"读成"机制做的"。**
  📌 **下一位要记住的判别法**：cron 自己触发时 prompt 是**孤零零一段**；
  人手工重发时后面常跟着一句人话（本次是"继续"）。**但这只是启发式，不是证据** ——
  **要确证只能问人，或看 `CronList` 的下一次触发时刻对不对得上。**

- 🔴🔴 **由此暴露一条结构性缺口，比误记本身更值钱：第 1 层的节奏与守卫会把巡检系统性地推到午夜。**
  第 1 层排的是 `13 */8 * * *` ⇒ 本地 **00:13 / 08:13 / 16:13**；守卫是 `<20h 跳过`。
  以第 8 次（`08-25 00:02`）为起点算，三次触发分别只隔 **0.2 h / 8.7 h / 16.2 h，全部 <20 h，全部跳过**,
  **第一次能放行的是次日 00:13（24.2 h）。**
  ⇒ **单靠第 1 层，巡检间隔会稳定落在 ~24.2 h 且时刻永远贴着午夜**，
  而"当天晚上做一次"这件事**它做不到**。本次之所以在 21:11 做成，**完全是第 2 层加人促成的。**
  ⚠️ **本轮实测还少了一次触发**：`16:13` 那次**根本没有到达**（`08:13` 那次到了，`21:10` 那次是人手工重发）。
  **原因未查明** —— 可能是会话不空闲、可能是别的，**本轮没有证据，不要猜**。
  ⇒ **断档史照记：46.6 h → 121.0 h → 23.4 → 23.6 → 58.9 h → 38.0 h → 21.2 h**，
  但**这一次的 21.2 h 不能记在定时器账上**。

- ✅ **ⅩⅨ.3 留的那个未决判据，本轮第一次可读，答案是好的那个。**
  改成必须点掉的对话框之后的**第一次自然触发**（不是 `kickstart`）：`20:54:51 fired → 21:03:12 ACK`。
  ⇒ **"每天到点也弹得出来、人也点得到"这句话第一次有了证据**，不再只有手动触发那一次。
  ⚠️ **两条边界照记**：① 排的是 **20:47**，实际 `20:54:51` 响，**晚了 7.9 分钟**（机器休眠后 launchd 补触发，
  与 `08-24` 那次 `21:00:38` 同一形态）—— **别把日志里的时刻当成排程时刻**；
  ② ACK 距触发 **8.4 分钟**，说明人不是当场看见的。**"送达"成立，"即时"不成立。**

- **判据 1**：窗口内 `prompt_submit` 共 **420** 行，`error !== null` **0 命中 ✅**。

- **判据 2 的成群检查（本轮重点）**：>2000ms 仍是 **4 条，逐条与上次完全相同** ——
  `08-11 09:26:59 / 2111.663541ms`、`08-11 21:16:03 / 2525.063584ms（stop）`、
  `08-11 22:25:34 / 2864.040875ms`、`08-13 21:20:35 / 2265.728667ms`。**max 未变，仍是 2864.040875ms。**
  增量 **65 行 / `prompt_submit` 32 行、0 条新 >2000ms**，增量内 max **1735.364959ms**
  （`08-25 09:07:27`，仍 <2000ms、不进名单）。
  ⇒ 自 `08-13` 那条起**连续第 6 次零新增**，累计跨 **12.0 天、176 次 `prompt_submit`**
  ⇒ **"高值稀疏、未见上移"继续成立。看的是有没有成群，不是有没有破 max。**
  📌 增量内 max 的走势：`1832 → 1593 → 1558 → 1735`ms —— **在 1.5–1.9 秒之间来回，无趋势**，
  **不作为判据**，只是同一把尺子缩到增量的补充读数。

- **判据 3**：**不可操作化、未执行（人类裁决 2026-08-10，补遗 2）**。照写，**不得默认它通过。**

- **原始 `n` = 287**（上次 263）。文件 **7368 行**、**0 解析失败**、`ts` 严格单调 **0 处逆序**、
  **无 `.1` 轮转**（`~/.claude/ccmem/` 下只有 `metrics.jsonl`，1.8M）。最后一行 `21:10:26`。
  窗口已开 **379.3 h**。
  🔴 **原始 `n` 越过 150 仍不等于闸门开了** —— 闸门要**剔除后**的 `n`（补遗 3），
  而它只有跑冻结脚本才算得出来，**那一跑同时就是读数**。**本轮未跑。**

- ✅ **尺子自检**：脚本是上一轮那份的**原样复制，一行未改**（`shasum -a256` 两份逐位相同，
  cutoff 走命令行参数）。卡回 `2026-08-25 00:02:16` **逐项复现第 8 次的记录**：
  `7303 行 / 0 解析失败 / 0 逆序 / 窗口内 807 行 / prompt_submit 388 / n=263 / 4 条 / max 2864.040875`，
  连增量 `47 行 · 23 ps · max 1558.315417ms` 都逐位吻合。

**外来负载取证（区间 `08-25 00:02:16 → 21:11:40`，共 21.2 h）：**

1. 🔴🔴 **第六次巡检，第六次抓到外来负载 —— 但这一次的来源与前五次都不同。**
   前五次抓到的都是别的仓库在跑测试（三次是 `loop/ccloop`）。**本次一次都没有**，
   抓到的是 **Claude Code 自己在升级**：
   - **10 次 `npm view @anthropic-ai/claude-code@latest version --prefer-online`**
     （`18:08:49` / `18:41:17`×2 / `19:21:32`×2 / `19:53:51`×2 / `20:54:51`×2 / `21:07:10`，本地时刻），
     即自动更新检查在轮询；
   - 🔴 **1 次 `npm install --global @anthropic-ai/claude-code@2.1.245`**，本地 **`21:07:12`**，
     cwd `/Users/biran` —— **距本次巡检 cutoff（`21:11:40`）只有 4.5 分钟，
     距 `metrics.jsonl` 最后一行（`21:10:26`）只有 3.2 分钟。**
     全局安装是真实的网络 + 磁盘 + postinstall 负载，**且它就压在本次读数的时刻上。**
   ⇒ **"外来负载是常态"这个结论继续成立，但它的形状要改写**：
   **不只是别的仓库跑测试，还包括本机工具链自己的后台活动** —— 后者**与是否有别人在写代码无关，
   任何时刻都可能发生**。⚠️ **仍不触发窄义剔除**（只剔本仓库全量套件）
   ⇒ **批次表不动、`n` 不动、补遗 6 双臂不动。**
2. ⚠️ **弱（承 2026-08-21 的降级，本轮第六次实证）**：`~/.zsh_history` 在区间内**一条都没有**，
   🔴 **而同期确实发生过 2 次 `git push`** —— ccmem `00:43:38`（`d50173a`）与 `01:08:20`（`84d0e47`），
   均出自 `.git/logs/refs/remotes/origin/main`。⇒ **第六次实证，本条不得标 ✅强。**
3. ✅ **强（reflog，正向）**：区间内 ccmem **有 2 次 push，但都不是我推的** ——
   人类在 `00:43:38` 与 `01:08:20` 推了上一轮那批提交。
   ⇒ 再次印证"未 push"只对"**我没推**"成立。**本轮我未 push。**
4. ✅ **会话直接观测**：本会话（08-25 01:09 起）**未跑任何测试**（全量套件与单文件都没有）
   —— 做的是只读分析、写 W3 实现计划、重挂 cron。**本仓库未产生需剔除的跑批窗口。**
   ⚠️ **但 `08-25 01:1x → 20:5x` 这约 19.7 小时里本会话是空闲的，那段没有等价的直接观测。**
5. ⚠️ **零效力，仅作记录**：本仓库区间内有 2 个提交，都是 docs-only。
   **"提交全是 docs-only ⇒ 没跑套件"这个论证对全局文件不成立**（ⅩⅤ.2），**不要拿它当证据。**

📌 **本次会话在本机做过的动作，照纪律当场记（都不构成需剔除的跑批窗口）**：

| 时刻 | 内容 | 性质 |
|---|---|---|
| 01:1x–01:5x | 读 handoff / 设计 / 源码，写 `docs/superpowers/plans/2026-08-25-w3-threat-scan-bypass-suite.md` | 只读 + 文档写入 |
| 01:5x | `python3` 校验计划里内嵌的 29 条语料 JSON（**纯文本解析，未 import 扫描器**） | 只读，亚秒 |
| ~21:10 | `cp -r ~/.npm/_logs` 成快照 | 只读复制 |
| 21:11:40 | 巡检脚本（scratchpad 里的只读 `.mjs`，跑了 2 次：一道自检 + 正式一次） | 只读，亚秒 |
| 21:1x | 读 `~/.zsh_history` / 两个仓库的 `origin/main` reflog / `~/.ccmem-inspect-reminder.log` | 只读，亚秒 |

⚠️ 以上全是**亚秒级、非测试**的动作，**按补遗 3 的窄义不构成需剔除的跑批窗口**。
📌 **未跑任何测试**（全量套件与单文件都没有），**未跑冻结读数脚本 `…-readout.mjs`**，
**未跑 W3 语料**（计划里一行代码都没执行，窗口仍开着）。

**效力边界（本次逐条标）：**

- 读的是**活文件** `~/.claude/ccmem/metrics.jsonl`，不是冻结快照。
  对判据 1/2 这种布尔/离群判定可接受，**Task 5 读数仍必须先冻结**（预登记口径，不得放宽）。
- 巡检脚本是上一轮那份只读脚本的**原样复制**（scratchpad，未入库），**不是冻结的读数脚本** ——
  **冻结脚本本轮一次都没跑过**，它一跑就是读数。
- 🔴 **npm 快照只覆盖 `18:08:49 → 21:07:12`，共 3.0 小时，而区间是 21.2 小时** ——
  **`00:02 → 18:08` 这 18.1 小时的 npm 活动一份都读不到**（`logs-max=10`，被后来的日志挤掉了）。
  **那段是"没观测"，不是"没发生"。**
- ⚠️ **10 份 `npm view` 日志里没有 `cwd` 行**（该 verbose 级别不输出），
  **它们的工作目录无法从证据里认定**；只有 `21:07:10` 与 `21:07:12` 两份记着 `cwd /Users/biran`。
  归因靠的是 argv 与 title（`npm view @anthropic-ai/claude-code@latest version`），**不是 cwd。**
- ✅ **自伤教训继续执行且这次拿到了直接证据**：先 `cp -r` 再读，**并且没跑 `npm config get logs-max`**。
  🆕 **`logs-max=10` 这个值本轮是从日志自己的第 8 行读到的**（`verbose logfile logs-max:10 dir:…`），
  同一份日志第 10 行还写着 `silly logfile start cleaning logs, removing 2 files`
  ⇒ **"这个证据源会被观测动作污染"从推断变成了直接观测，而且取证过程零污染。**
- `08-25 01:1x → 20:5x` 约 19.7 小时无会话观测；断档期的本机负载**没有任何等价证据**。
